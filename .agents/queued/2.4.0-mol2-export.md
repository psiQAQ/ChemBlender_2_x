# ChemBlender 2.4.0 Native MOL2 Export

## Execution Cursor

- Goal ID: `CB240-MOL2-EXPORT-T1`.
- State: `not_started`.
- Current task: `Awaiting explicit activation of deterministic native MOL2 export`.
- Planning baseline: `6cba3286a058ec94621985b7adaa7a40ea258e6f`.
- Evidence: `docs/quantum-visualization/2.4.0/candidate-intake.md`.
- Plan: `docs/superpowers/plans/2026-08-01-chemblender-2.4.0-mol2-export.md`.
- Existing readiness contract: `docs/quantum-visualization/2.3.0/specs/mol2-export-p1.md`.
- Required subgoals:
  - `deterministic-core-writer`;
  - `explicit-loss-preview`;
  - `atomic-cancellation-boundary`;
  - `semantic-native-reimport`;
  - `core-capability-publication`.
- Allowed product scope: native MOL2 exporter module, exporter public surface,
  MOL2 exporter tests, generated capability documents and same-change architecture
  documentation.
- Stop boundary: No product implementation has started. Do not add Blender export UI,
  PDB/PQR/Cube export, dependencies, model/schema changes or release changes in Task 1.
- Activation rule: after Scope Discovery is completed and committed, move this file to
  `.agents/active/`, record that checkpoint SHA, set `State` to `in_progress`, then follow
  the plan from its first unchecked task.
- Resume rule: read `AGENTS.md`, `.agents/README.md`, this cursor, the candidate intake,
  the implementation plan and the readiness contract before changing product files.
- Remote policy: No push, PR, remote modification, tag or Release operation belongs to
  the local Task 1 checkpoint without a new explicit authorization.
- Remote CI: `Not Run`.
