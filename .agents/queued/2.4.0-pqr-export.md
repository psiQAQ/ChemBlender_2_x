# ChemBlender 2.4.0 Native PQR Export

## Execution Cursor

- Goal ID: `CB240-PQR-EXPORT-T5`.
- State: `not_started`.
- Current task: `Task 1 — Freeze native PQR export contract`.
- Selected task: `Task 5 — Deterministic native PQR export`.
- Selected by: `CB240-TASK5-SCOPE-DISCOVERY`.
- Baseline: `d5028aa5d8568a44181b822293fbe62462d9a496` plus the Task 5
  Scope Discovery checkpoint.
- Plan:
  `docs/superpowers/plans/2026-08-02-chemblender-2.4.0-pqr-export.md`.
- Evidence:
  `docs/quantum-visualization/2.4.0/task5-candidate-intake.md`.
- Existing readiness boundary:
  `ChemBlender/core/exporters/pdb_readiness.py::pqr_export_readiness`.
- Required subgoals:
  - `public-pqr-writer-contract`;
  - `deterministic-10-11-field-output`;
  - `element-inference-identity`;
  - `loss-preview-confirmation`;
  - `atomic-cancellation-cleanup`;
  - `semantic-native-reimport`;
  - `core-capability-publication`;
  - `blender-package-qualification`;
  - `exact-head-remote-gate`.
- Stop boundary: Complete native PQR core export only. PQR UI remains unstarted;
  Cube, Reader API stable, model/schema/dependency/version and Release work stay
  unchanged.
- Activation rule: A later explicit instruction moves this cursor to
  `.agents/active/`; do not modify runtime during Scope Discovery.
- Resume rule: After activation, read this cursor, the plan and candidate
  intake, then start from the first unchecked plan item.
- Remote CI: `Not Run`.
- Remote policy: No push, PR or merge until separately authorized during the
  implementation remote gate.
