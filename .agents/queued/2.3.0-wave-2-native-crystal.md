# ChemBlender 2.3.0 Wave 2 — Native Crystal

## State

Queued behind Wave 1.

## Goal

交付 `2.3.0-beta.1` 的Gemmi CIF、原生POSCAR/CONTCAR、晶体视图和API/schema冻结。

## Success Criteria

- Gemmi wheel固定版本/URL/SHA/license并通过隔离生命周期。
- 无spglib环境可完整导入CIF并保留envelope、occupancy和Uij。
- POSCAR保留scale、坐标模式和Selective Dynamics。
- 文件声明对称性与spglib派生结果并存。
- CIF受控导出、POSCAR round-trip。
- Reader API v1 RC和sidecar schema冻结。

## Constraints

- spglib保持可选。
- 不打包ASE/pymatgen。
- 不在beta后做破坏性schema变化。

## Verification

真实CIF/POSCAR fixtures、Gemmi wheel inventory、Blender crystal smoke、size gate和schema migration tests。
