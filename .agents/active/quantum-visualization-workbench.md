# Quantum visualization workbench

Current authority: [owned task plan](../../.planning/2026-09-08-quantum-visualization-workbench/task_plan.md).

Implement approved first-release A-D: density-only property geometry and old-view rebuild, orbital/observable worker UI, affine slices/profiles, persistent export and example. Later professional/periodic/GPU work remains a roadmap.

The root agent owns task_plan.md, findings.md and progress.md. Recovery reads those files and verifies live Git/runtime state. Existing user Blender scene is unsaved; validation uses separate background instances.

Phases A, B and C are complete. Real FCHK/Molden/UHF numerical and Blender UI gates passed, including same-project source switching, cancellation and cleanup. Affine sampling and native Blender View/CSV gates passed. Phase D is implementing batch image export and a real example with full lifecycle validation; final installation qualification remains pending.

Reference submodules are downloaded and pinned. User approved the project-cache Python environment; Python 3.12.13 and all eight pinned packages are installed and compatible. No remote write or release is authorized.
