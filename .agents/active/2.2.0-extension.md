# ChemBlender 2.2.0 Extension Migration

## Goal

Deliver ChemBlender 2.2.0 as a Blender 5.1 extension under `ChemBlender/`, with reproducible offline RDKit packaging and real install/reload verification.

## Confirmed Constraints

- `v2.1.1` is the final legacy add-on release.
- 2.2.0 is extension-only and Windows x64 initially.
- The repository root is the development workspace; `ChemBlender/` is the extension root.
- NumPy comes from Blender.
- RDKit is a manifest wheel downloaded locally or by CI and never tracked.
- Runtime import/register code performs no package or network installation.
- No remote write occurs without explicit approval.

## Completed Gates

- Preserved the pre-rebuild spike as `archive/extension-spike-20260707`.
- Rebuilt 2.1.1 from `78c2d8d` with exactly two compressed `.blend` files and the version tuple.
- Verified both assets in Blender 5.1.2: 174 and 171 node groups.
- Fast-forwarded local maintained `main` and created local `v2.1.1`.
- Established the tracked Agent knowledge structure for the 2.2.0 branch.

## Current Phase

Build and validate the 2.2.0 extension package with its locally downloaded RDKit wheel, then add the reproducible CI workflow.

## Next Action

Execute Task 7 in `docs/superpowers/plans/2026-07-21-chemblender-2.1.1-and-2.2.0-rebuild.md`.

## Verification Record

- Blender runtime: 5.1.2 on Windows.
- `Chem_Nodes.blend`: 1,189,463 bytes, 7 objects, 174 node groups.
- `Chem_Nodes_En.blend`: 1,209,480 bytes, 8 objects, 171 node groups.
- Legacy ZIP: `D:\workspace\ChemBlender-2.1.1.zip`, 21 entries, no manifest/wheel/tests/Agent docs.
- Legacy ZIP SHA-256: `f90a2b783b37f8d3366c3a6a49fcdaa193d4e2d4c152fcde07315bacb0bbfac0`.
- Repository contract: 4 tests passed with Blender's Python 3.13.
- Extension source: compiled successfully; existing invalid-escape warnings remain in `mesh.py` and `read.py`.
