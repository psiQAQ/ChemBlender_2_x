# ChemBlender 2.3.0 Wave 0 — Platform Foundation

## State

Active. Wave 1–4 remain queued.

## Live Baseline

- Branch: `feat/2.3.0-wave0-platform-foundation`
- Full starting SHA: `ba6d0a7a72fece7c6a1336a0e931f2f090d76e2c`
- Blender version: `5.1.2`
- Blender executable: `C:\Program Files\Blender Foundation\Blender 5.1\blender.exe`
- Bundled Python: `C:\Program Files\Blender Foundation\Blender 5.1\5.1\python\bin\python.exe`
- System: `Windows`
- Extension repositories:
  - `blender_org`: `C:\Users\ustcw\AppData\Roaming\Blender Foundation\Blender\5.1\extensions\blender_org` (enabled)
  - `user_default`: `C:\Users\ustcw\AppData\Roaming\Blender Foundation\Blender\5.1\extensions\user_default` (enabled)
  - `system`: `C:\Program Files\Blender Foundation\Blender 5.1\5.1\extensions\system` (enabled)
  - `lab_blender_org`: `C:\Users\ustcw\AppData\Roaming\Blender Foundation\Blender\5.1\extensions\lab_blender_org` (enabled)
- MCP recovery: Passed. No Blender process was running; Blender 5.1 was started and `127.0.0.1:9876` became available before the successful live query.
- Current test status:
  - Passed: `python -m unittest tests.test_quantum_visualization_docs -v` (11 tests).
  - Passed: `python -m compileall -q ChemBlender tests` (two pre-existing `invalid escape sequence` warnings).
  - Passed: planning bundle verifier (`ok: true`, 21 plans, 6 specs, 12 ADRs, 5 queues).
- Audit corrections: None. Runtime files remain at the audited `1cf492f40d5fa8799fa964b8dfc914ab5ecbec4c` state.
- Verification status: Not Run
- Current implementation plan: `docs/superpowers/plans/2026-07-23-chemblender-2.3.0-wave-0-core-modularization.md`

## Execution Cursor

- Goal ID: `W0-ITD-T6-PROJECT-TRANSACTION`
- Plan: `docs/superpowers/plans/2026-07-23-chemblender-2.3.0-wave-0-import-transaction-diagnostics.md`
- Current task: `Task 6 — Implement ProjectTransaction`
- State: `completed`
- Baseline commit: `11d9ee1322dbebbe0a615a9c06d6dfc2a67bf936`
- Planning correction commits:
  - `56ddba5e5b738e53762df14b48b8a4807e08bf77`
  - `d910ce7c69af0369bfe2a33b46b3ca2fcf058824`
  - `6304882c7a5ebecd0cb3e7bd60b5d4bac5b732a0`
- Completed implementation commit: `997d9f4b2caa34879c068669e26f0f0fb7d8a88d`
- RED evidence:
  - Failed as expected: initial transaction test raised `ImportError` because `GroupingDecision` and the transaction API did not exist.
  - Failed as expected: publication ownership tests rejected the transfer option and could not obtain the exact verified project.
  - Failed as expected: ten review probes produced three failures and four errors for cross-batch forward references, conflict/group mapping, grouping ID derivation, opaque model values, cleanup reporting and result validation.
  - Failed as expected: two final review probes showed `LINK_EXISTING` positionally mapping unrelated entities and conflict resolution dropping view intent IDs.
- GREEN evidence:
  - Passed: final transaction, project and publication tests (52 tests).
  - Passed: final related Task 6 regression suite (173 tests).
  - Passed: `python -m unittest discover -s tests -p "test_*.py" -v` (521 tests, 27 skipped, 0 failed).
  - Passed: `python -m compileall -q ChemBlender tests`.
  - Passed: pure import boundary; `bpy`, `cclib`, `iodata`, `gbasis`, `ase` and `pymatgen` were not loaded.
  - Passed: committed `.cbq` v0.1 fixture SHA-256 table (7/7 files) and reopen migration to the current model.
  - Passed: `git diff --check`; implementation worktree was clean.
- Review:
  - Passed: final specification compliance review.
  - Passed: final code quality review.
  - Findings after final review: 0 Critical, 0 Important, 0 Minor.
- Known limitations:
  - Blender runtime verification: `Not Run: Task 6 is a pure-Python transaction and sidecar task`.
  - A `SourcePreview` with more than one staged batch fails closed until an explicit multi-batch source contract is defined.
  - `NEW_REVISION` with candidates from more than one existing source fails closed until the decision contract supplies an explicit target.
  - `LINK_EXISTING` does not positionally map created entities; confirmed entity-based grouping evidence fails closed without typed semantic mapping.
  - Default view plan UUIDs are returned as unverified intent; the Blender caller must validate and apply them and may report data committed/view failed.
  - Failure to close the previous project after a verified commit is returned in `cleanup_warnings` without misreporting the committed transaction as failed.
- Next task: `Task 7 — Add deterministic diagnostic summaries and exports`.
- Stop boundary: Task 7 has not started; Task 8 and Wave 1–4 remain inactive.
- Remote policy: no push without explicit user authorization.
- External-write policy: current worktree and `.agents/archive/` only.

## Goal

交付 `2.3.0-alpha.1` 的项目、来源、导入事务、Reader API 0.x、显式注册和最小用户界面闭环。

## Success Criteria

- 现有XYZ/Cube fixture可经Import Preview提交到Session Project。
- 可创建默认View、保存`.cbq`、关闭并重开恢复。
- 重复冲突、Balanced Recovery和诊断在UI可见。
- `ChemBlender.core`公共导入兼容；模型内部模块化。
- auto-load不递归加载pure core和optional stacks。
- prerelease version probe和动态artifact命名有验证。

## Constraints

- 不新增Wave 1格式。
- 不打包Gemmi；Gemmi属于Wave 2。
- 不同时激活其他Wave。
- 每个implementation plan独立TDD、提交和review。

## Plans

按 `docs/superpowers/plans/2026-07-23-chemblender-2.3.0-master-sequencing.md` 中的Wave 0顺序执行。

## Verification

普通CPython tests、文档契约、Blender 5.1.2 validate/build、隔离install、Quick Import smoke、sidecar reopen和`git diff --check`。
