# ChemBlender 2.3.0 Wave 0 — Platform Foundation

## State

Active. Wave 1–4 remain queued.

## Live Baseline

- Branch: `feat/2.3.0-wave0-platform-foundation`
- Full starting SHA: `ba6d0a7a72fece7c6a1336a0e931f2f090d76e2c`
- Blender version: `5.1.2`
- Blender executable: `C:\Program Files\Blender Foundation\Blender 5.1\blender.exe`
- Bundled Python: `C:\Program Files\Blender Foundation\Blender 5.1\5.1\python\bin\python.exe`
- System: `Windows`
- Extension repositories:
  - `blender_org`: `C:\Users\ustcw\AppData\Roaming\Blender Foundation\Blender\5.1\extensions\blender_org` (enabled)
  - `user_default`: `C:\Users\ustcw\AppData\Roaming\Blender Foundation\Blender\5.1\extensions\user_default` (enabled)
  - `system`: `C:\Program Files\Blender Foundation\Blender 5.1\5.1\extensions\system` (enabled)
  - `lab_blender_org`: `C:\Users\ustcw\AppData\Roaming\Blender Foundation\Blender\5.1\extensions\lab_blender_org` (enabled)
- MCP recovery: Passed. No Blender process was running; Blender 5.1 was started and `127.0.0.1:9876` became available before the successful live query.
- Current test status:
  - Passed: `python -m unittest tests.test_quantum_visualization_docs -v` (11 tests).
  - Passed: `python -m compileall -q ChemBlender tests` (two pre-existing `invalid escape sequence` warnings).
  - Passed: planning bundle verifier (`ok: true`, 21 plans, 6 specs, 12 ADRs, 5 queues).
- Original audit baseline: `1cf492f40d5fa8799fa964b8dfc914ab5ecbec4c`.
- Last externally reviewed remote HEAD:
  `7078356c85bd02fcb0db23b01490f87f16abfb94`.
- Live Baseline is the historical activation baseline, not the current runtime HEAD.
- Completed Wave 0 plans: Core Modularization; Source / Session / Sidecar; Import Transaction / Diagnostics.
- Historical activation verification status: Not Run
- Current next implementation plan: `docs/superpowers/plans/2026-07-23-chemblender-2.3.0-wave-0-registration-ui.md`

## Execution Cursor

- Goal ID: `W0-REGISTRATION-UI-T1-EXPLICIT-ROOTS`
- Plan:
  `docs/superpowers/plans/2026-07-25-chemblender-2.3.0-wave-0-registration-task1-explicit-roots.md`
- Parent plan:
  `docs/superpowers/plans/2026-07-23-chemblender-2.3.0-wave-0-registration-ui.md`
- Current task: `Task 1 — Introduce explicit registration roots`
- State: `completed`
- Reviewed baseline:
  `7078356c85bd02fcb0db23b01490f87f16abfb94`
- Planning commit:
  `3a8b635db9d475fb3d86fc83bd48968a790aac74`
- Implementation commit:
  `eadd8ac6f6bcf0dd810f302821e19365b6ef7d38`
- Allowed runtime files:
  - `ChemBlender/runtime/registration.py`
  - `ChemBlender/runtime/__init__.py`
  - `ChemBlender/runtime/reader_api_bridge.py`
  - `ChemBlender/__init__.py`
  - `ChemBlender/auto_load.py`
  - `.gitattributes`
- Allowed tests/docs:
  - `tests/test_registration_contract.py`
  - `tests/fixtures/registration/legacy-registration-inventory.json`
  - `tests/test_reader_api_bridge_contract.py`
  - `tests/test_repository_contract.py`
  - `tests/blender_smoke.py`
  - `.agents/reference/code-architecture-guide.md`
  - `tests/test_quantum_visualization_docs.py`
  - `.agents/active/2.3.0-wave-0-platform-foundation.md`
  - `docs/superpowers/plans/2026-07-25-chemblender-2.3.0-wave-0-registration-task1-explicit-roots.md`
- RED evidence:
  - Passed as expected: the initial registration contract ran 11 tests and
    failed with 11 `ModuleNotFoundError` errors because
    `ChemBlender.runtime.registration` did not exist.
  - Passed as expected: review hardening ran 17 tests and observed five
    failures plus one error for callback self-rollback, cleanup notes,
    residual retry, helper exception propagation and interface annotations.
  - Passed as expected: safe-helper hardening ran 21 tests and observed two
    failures plus two errors for pre-existing/already-unregistered class
    ownership semantics.
- GREEN evidence:
  - Passed: focused registration, Reader API bridge, repository and
    documentation contracts (47 tests).
  - Passed: full discovery (732 tests, 27 skipped, 0 failed).
  - Passed: `python -m compileall -q ChemBlender worker tests`.
  - Passed: `git diff --check` and standard commit-range
    `git diff --check 3a8b635db9d475fb3d86fc83bd48968a790aac74..eadd8ac6f6bcf0dd810f302821e19365b6ef7d38`.
- Legacy inventory: Passed — Blender 5.1.2 baseline recorded 95 discovered
  modules, 56 registered classes, two module callbacks, one handler and
  13 menu callbacks, with no owned residue after unregister.
- Explicit registration equivalence: Passed — the implementation uses the
  exact inventory-backed explicit roots and no ordinary recursive package
  scan.
- Atomic rollback and retry: Passed — failing callbacks clean their own
  partial state, cleanup preserves the original failure, and only residual
  owned state is retained for retry.
- Class helper lifecycle: Passed — pre-existing classes are not acquired,
  already-unregistered classes are released without residual state, and
  unknown Blender errors propagate.
- Reader API lifecycle: Passed — registry, built-in readers, public model
  identities and external reader continuity survive disable/enable; one owned
  handle is published and removed per cycle.
- Blender native extension validate/build: Passed with Blender 5.1.2.
- Built ZIP audit: Passed — 110 entries, 27,337,028 bytes, SHA-256
  `ed5e0c2b99623e5b2c912a49e327c1b936a823e19b1e4905e1c2c33224839a66`;
  registration source is present and tests, scripts and future UI are absent.
- Short-path isolated install: Passed at `D:\cb23r7`, including two lifecycle
  cycles, frozen inventory equality, Reader API identity, optional-stack
  isolation and RDKit runtime checks.
- Default `user_default` install: Passed (`FINISHED`); two disable/enable
  cycles each ended with 45 formal classes, two callback modules, one
  trajectory handler and one owned Reader API handle.
- Artificial long-path verification:
  `Not Run: the pre-existing 276-character Windows extraction-path failure is
  a recorded known limit and is not resolved by Task 1`.
- Independent reviews: Passed — specification compliance and code quality
  both report zero Critical, Important and Minor findings.
- Blender runtime warnings: the pre-existing generated-menu identifier,
  invalid escape sequence and RDKit DLL-lock cleanup warnings remain known
  limitations; the verified processes exited successfully.
- Next task:
  `Registration/UI Task 2 — Add Blender ProjectSession manager`
- Stop boundary: Registration/UI Task 2 has not started;
  `ChemBlender/ui/session.py`, Release Groundwork and Wave 1–4 remain
  unstarted.
- Resume rule: after context compaction or a new session, read this Execution
  Cursor and the Registration/UI parent plan before editing. Begin Task 2 only
  when explicitly continuing Wave 0 work.
- Remote policy: no push without a new explicit user authorization.
- External-write policy: current worktree and `.agents/archive/` only.
- Known Windows packaging risk: a prior artificially long isolated extraction
  path reached 276 characters and failed while a short-path isolated install
  passed. Do not claim this path-length risk is resolved in Task 1.

## Goal

交付 `2.3.0-alpha.1` 的项目、来源、导入事务、Reader API 0.x、显式注册和最小用户界面闭环。

## Success Criteria

- 现有XYZ/Cube fixture可经Import Preview提交到Session Project。
- 可创建默认View、保存`.cbq`、关闭并重开恢复。
- 重复冲突、Balanced Recovery和诊断在UI可见。
- `ChemBlender.core`公共导入兼容；模型内部模块化。
- auto-load不递归加载pure core和optional stacks。
- prerelease version probe和动态artifact命名有验证。

## Constraints

- 不新增Wave 1格式。
- 不打包Gemmi；Gemmi属于Wave 2。
- 不同时激活其他Wave。
- 每个implementation plan独立TDD、提交和review。

## Plans

按 `docs/superpowers/plans/2026-07-23-chemblender-2.3.0-master-sequencing.md` 中的Wave 0顺序执行。

## Verification

普通CPython tests、文档契约、Blender 5.1.2 validate/build、隔离install、Quick Import smoke、sidecar reopen和`git diff --check`。
