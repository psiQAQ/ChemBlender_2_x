# ChemBlender 2.4.0 PDB Export UI

## Execution Cursor

- Goal ID: `CB240-PDB-EXPORT-UI-T4`.
- State: `in_progress`.
- Current task: `Task 1 — Activate Task 4 — PDB Export UI`.
- Selected by: `CB240-TASK4-SCOPE-DISCOVERY`.
- Baseline: `d1ff05b82d1d7566c9f34c99c13e81a1ef59e234`.
- Plan:
  `docs/superpowers/plans/2026-08-02-chemblender-2.4.0-pdb-export-ui.md`.
- Existing core contract:
  `docs/quantum-visualization/2.4.0/pdb-export-contract.md`.
- Completed tasks: `none`.
- RED evidence:
  - activation routing: `1 failure, 1 error`; active cursor absent and queued
    cursor still present.
- Baseline verification:
  - focused suite: `96 Passed` with Blender Python and the installed
    extension shared site-packages;
  - the initial direct Blender Python run had `7` existing SMILES errors
    because RDKit was not on `sys.path`; no dependency was installed or
    changed.
- Required subgoals:
  - `selected-pdb-entity-projection`;
  - `pdb-format-choice`;
  - `loss-preview-confirmation`;
  - `background-atomic-export`;
  - `project-browser-capability`;
  - `installed-pdb-export-reimport`.
- Stop boundary: Complete PDB Export UI only. PQR, Cube, Reader API stable,
  model/schema/dependency/version and Release work remain unstarted.
- Resume rule: Read this cursor and the plan, then continue from the first
  unchecked task without repeating completed tasks.
- Remote policy: No push or PR until local verification and review pass.
