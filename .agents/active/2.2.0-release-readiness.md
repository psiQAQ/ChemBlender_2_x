# ChemBlender 2.2.0 Release Readiness

## Authority

Execute and update `docs/superpowers/plans/2026-07-21-chemblender-2.2.0-release-readiness.md` until every task is verified.

## Goal

Produce a focused repository and extension package, prove isolated and real Blender 5.1.2 installations, and obtain a green GitHub Actions run from a draft pull request.

## Confirmed Boundaries

- Keep `unittest`; do not add pytest without a demonstrated need.
- Do not bundle Pillow while extension code does not use PIL-dependent behavior.
- Use Blender's bundled NumPy and Requests and the pinned RDKit wheel.
- Declare actual file/network permissions; never install dependencies at runtime.
- Generated wheels, ZIPs, caches, and Blender test profiles remain ignored.
- Push `main`, `v2.1.1`, the archive branch, and the feature branch; do not push the redundant release branch.
- Do not merge the draft pull request, publish 2.2.0, or delete the old remote snapshot branch without a separate decision.

## Current Phase

Remove confirmed ignored debris, then harden the GitHub Actions workflow.

## Next Verification

Preview and remove only the explicit ignored targets in Task 4 while retaining the pinned RDKit wheel.
