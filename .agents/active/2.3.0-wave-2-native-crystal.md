# ChemBlender 2.3.0 Wave 2 — Native Crystal

## State

Gemmi dependency gate completed. Wave 1 remote integration and Wave 2
ancestry reconciliation completed. Wave 2 is the only active Wave.

## Goal

Deliver `2.3.0-beta.1` with native crystal models, bundled Gemmi CIF,
dependency-free POSCAR/CONTCAR, crystal views and the API/schema freeze.

## Live Baseline

- Branch: `feat/2.3.0-wave2-native-crystal`.
- Starting Wave 1 SHA:
  `1bbab2101c71b9954b198678f6a1026e9eb2a440`.
- Blender baseline: `5.1.2`, Windows x64.
- Bundled Python:
  `C:\Program Files\Blender Foundation\Blender 5.1\5.1\python\bin\python.exe`.
- Baseline focused crystal/storage/API tests: `77/77 Passed`.
- Fresh checkout full baseline: `1411 Ran`, `28 Skipped`,
  `1 Failed`, `1 Error`.
- Baseline failures:
  Windows `core.autocrlf=true` changed the shared MOL fixture bytes, causing
  the CRLF-construction test and the deterministic 10k SDF hash test to use
  checkout-dependent input. The Crystal Pre-Gate must make these test inputs
  byte-stable before its final full gate.
- Remote CI: `Not Run`.

## Execution Cursor

- Goal ID: `W2-TASK1-GEMMI-DEPENDENCY-GATE`.
- Current task:
  `Wave 1 integration gate completed; Task 2 — Gemmi adapter implementation
  is next`.
- Plan:
  `docs/superpowers/plans/2026-07-29-chemblender-2.3.0-wave2-gemmi-dependency-gate.md`.
- Parent plan:
  `docs/superpowers/plans/2026-07-23-chemblender-2.3.0-wave-2-gemmi-cif.md`.
- State: `completed`.
- Reviewed baseline:
  `b211ddc0316f854b5c960ba83cea7b35d7d84f00`.
- Required subgoals:
  - `official-cp313-windows-wheel`
  - `exact-version-and-sha-lock`
  - `lazy-import-boundary`
  - `manifest-and-workflow-packaging`
  - `blender-bundled-python-import`
- Completed tasks:
  - `official-cp313-windows-wheel`
  - `exact-version-and-sha-lock`
  - `lazy-import-boundary`
  - `manifest-and-workflow-packaging`
  - `blender-bundled-python-import`
- Planning commit:
  `263421b017be8946a5e316ee65b5bff5b139a5a6`.
- Implementation commit:
  `9735c517407eb7e5cbc0970098fab7ff13d606e3`.
- Manifest byte-lock test commit:
  `80a68d99e979591ab7878a5cd3c748597b640ca3`.
- Review-fix commit:
  `1d7cf9503256cb3764ec7833072bca7c421fb1e8`.
- RED evidence:
  initial dependency contracts `21 Ran / 5 Failed`; canonical alias
  regression `6 Ran / 1 Failed`.
- GREEN evidence:
  focused `72 Ran / 1 privilege-related Skipped / 0 Failed`; full
  `1430 Ran / 28 Skipped / 0 Failed`.
- Gemmi artifact:
  `gemmi-0.7.5-cp313-cp313-win_amd64.whl`, SHA-256
  `ad1f72ffa24adbfaf259e11471f6f071a668667f6ca846051f3bfea024fd337d`.
- Gemmi compressed/unpacked:
  `2,270,352 / 5,345,458 bytes`.
- Gemmi license:
  `gemmi-0.7.5.dist-info/licenses/LICENSE.txt`, MPL-2.0.
- Lazy core/Reader API/enable import: `Passed`.
- Blender Python 3.13.9 Gemmi import/version: `Passed`.
- Blender 5.1.2 validate/build/ZIP/lifecycle: `Passed`.
- Package SHA-256:
  `71fc5a890f36da4fba78bfabf77db757d6498fc559e5654801c807baab603de3`.
- Reviews: `SPEC PASS`; `QUALITY PASS`.
- Next plan:
  `docs/superpowers/plans/2026-07-23-chemblender-2.3.0-wave-2-gemmi-cif.md`.
- Next task:
  `Task 2 — Gemmi adapter implementation`.
- Stop boundary:
  Gemmi approval is complete. CIF reader, POSCAR, crystal UI, symmetry
  expansion and export remain unstarted.
- Resume rule:
  After compaction or a new session, read this cursor and the parent Gemmi/CIF
  plan before changing files. Start only Wave 2 Task 2 after user direction.
- Remote policy:
  The gate-closing Wave 2 push is explicitly authorized. No later push
  without new explicit user authorization.
- Remote CI:
  Wave 2 `Not Run`; Wave 1 integration evidence is recorded below.

## Wave 1 Integration Gate

- Gate ID: `W1-TO-MAIN-REMOTE-INTEGRATION`.
- State:
  `completed — merge, exact-head CI and Wave 2 ancestry reconciliation
  Passed`.
- Position:
  after the current Wave 2 task has a complete clean checkpoint and before
  the next Wave 2 task starts.
- Source branch: `feat/2.3.0-wave1-native-molecular-grid`.
- Target branch: `main`.
- Wave 2 branch: `feat/2.3.0-wave2-native-crystal`.
- Immediate recording boundary:
  this cursor update does not fetch, switch branches, create a PR, push Wave
  2 or perform any other remote write.
- Trigger conditions:
  - current Wave 2 task implementation and documentation are complete;
  - required tests and reviews Passed;
  - the current changes have a complete checkpoint commit;
  - the Wave 2 worktree is clean;
  - no agent is modifying the Wave 1 branch or its worktree.
- Trigger preflight:
  - run `git fetch origin --prune`;
  - verify the Wave 1 local and remote heads are identical;
  - verify `origin/main` is an ancestor of the Wave 1 head;
  - verify the Wave 1 head is an ancestor of the current Wave 2 head;
  - query existing Wave 1-to-`main` PRs before creating one.
- Divergence policy:
  stop remote writes and report exact SHAs, ancestry and PR state when any
  preflight assumption fails. Do not rebase, force-push or rewrite history.
- Authorized remote action:
  when preflight passes, create or reuse the Wave 1-to-`main` PR and monitor
  its CI. This authorization does not include merging.
- CI evidence contract:
  - required workflow/check `headSha` must equal the exact Wave 1 PR head;
  - wait for every required check to reach a terminal state;
  - record PR number/URL, workflow run ID/URL, verified head SHA and job
    conclusions;
  - never substitute an old run, another branch run or an artifact for exact
    head-SHA CI evidence.
- CI failure policy:
  do not merge. Classify the failure as Wave 1 regression, workflow failure
  or environment failure and report the failed job and key log evidence.
  Never patch Wave 1 from the Wave 2 worktree. Any new Wave 1 commit
  invalidates the old CI evidence.
- Merge authorization:
  granted by the user for the exact green Wave 1 head. The source branch was
  not deleted.
- Authorized merge method after approval:
  ordinary merge commit only; no squash or rebase merge.
- Post-merge verification:
  fetch with prune, verify the Wave 1 head is an ancestor of `origin/main` and
  remains an ancestor of Wave 2. Do not rebase Wave 2 or merge `main` into it.
  Fast-forward a local clean, unused `main` worktree only when safe.
- Required completion evidence:
  - PR number/URL:
    `#3 — https://github.com/psiQAQ/ChemBlender_2_x/pull/3` — `MERGED`;
  - CI fix commits:
    - `b04eacb088100b391ff75604ea1c6de1fb9c5175` — stage the verified
      RDKit wheel for Blender-Python unit tests without global installation;
    - `36ac8eaf9c1952a3835f80eae1c75c0edaf527fa` — keep raw MOL/SDF
      fixture line endings stable on Windows checkout;
  - final CI run ID/URL:
    `30370298027 — https://github.com/psiQAQ/ChemBlender_2_x/actions/runs/30370298027`;
  - verified Wave 1 head SHA:
    `36ac8eaf9c1952a3835f80eae1c75c0edaf527fa`;
  - CI job:
    `build` (`90312088709`) — `success`;
  - CI coverage:
    unit tests, native extension validate/build, isolated Blender install
    smoke and artifact upload all Passed;
  - post-merge `main` CI:
    `30370680117 — https://github.com/psiQAQ/ChemBlender_2_x/actions/runs/30370680117`
    verified merge commit `56ec631557ea9e178f76995dd6a210ba26f77efe`
    with job `90313403249` — `success`;
  - prior invalidated runs:
    - `30365580109` at `1bbab210...` failed because RDKit was not importable;
    - `30369801127` at `b04eacb...` passed RDKit bootstrap, then exposed two
      Windows checkout line-ending failures;
  - merge commit SHA:
    `56ec631557ea9e178f76995dd6a210ba26f77efe`;
  - post-merge ancestry:
    - final Wave 1 head is an ancestor of `origin/main`: `Passed`;
    - ordinary Wave 1-to-Wave 2 merge commit:
      `9d8d51412aa5ec1ce2ebad8128b09d3731a0082f`;
    - merge parents:
      `8b7a6c54e915ceafbe096c303bca2e5f77ef330d` and
      `36ac8eaf9c1952a3835f80eae1c75c0edaf527fa`;
    - final Wave 1 head is an ancestor of current Wave 2: `Passed`;
    - focused merge regression:
      `110 Ran / 1 privilege-related Skipped / 0 Failed`;
    - full merge regression:
      `1431 Ran / 26 Skipped / 0 Failed`;
    - compileall, optional-import audit and `git diff --check`: `Passed`;
    - Blender 5.1.2 native validate/build, exact ZIP inventory/CRC and
      isolated lifecycle: `Passed`;
    - package:
      `chemblender-2.3.0-alpha.1.zip`, `29,832,390` bytes, SHA-256
      `71fc5a890f36da4fba78bfabf77db757d6498fc559e5654801c807baab603de3`;
    - no Wave 2 rebase, `main` merge or force-push was performed;
    - pushed Wave 2 remote equality and ancestry are verified live after this
      gate-closing cursor checkpoint.
- Gate exit:
  `Passed`; Wave 2 Task 2 is unblocked but remains unstarted.

## Success Criteria

- The exact official Gemmi CPython 3.13 Windows x64 wheel is pinned.
- Manifest, CI download, local preflight and artifact inventory agree.
- Core, Reader API and extension enable remain Gemmi-lazy.
- Blender 5.1.2 can import the packaged wheel without global installation.
- Full tests, compileall, validate/build and lifecycle smoke pass.

## Constraints

- Gemmi is a bundled adapter dependency, not a scientific model.
- spglib remains optional.
- ASE and pymatgen are not bundled.
- Project and sidecar schema versions do not change.
- CIF parsing, POSCAR, crystal UI, symmetry expansion and export are not
  started by this gate.

## Verification

Focused TDD, full Blender-Python tests, compileall, optional-import audit,
native extension validate/build, exact ZIP/artifact audit, isolated Blender
lifecycle, direct bundled-Python import and independent specification/quality
review.
