# ChemBlender 2.5.0 Public Delivery

## Authority

用户已批准实现 2.5.0 对外交付计划：Standard `uv tool`、协议级 API、GUI 诊断、完整中英双语新 SOP、旧 SOP 归档、本机 Blender 私有及真实 profile 全公开面验收、可发布制品和稳定提交。

## Current State

- Branch: `release/2.5.0`
- Base: `d8f0306`
- User-owned change: `.gitignore`，必须保留且不提交。
- Completed predecessor: `.agents/completed/physical-quantity-visualization-sop.md`
- Active plan: `.planning/2026-09-10-2.5.0-public-delivery/task_plan.md`

## Next Gate

完成 Standard `uv tool` 发行形态、准确的 `capabilities`/`doctor`，并为 GUI 与 Blender Test Processor 补齐诊断信息。

## Latest Evidence

- routed worker 已改为目标 Python 的 `-I -m chemblender_prepare.worker.runner`，不再注入主环境源码或 `site-packages`。
- 同一 `0.1.0` wheel 已用 `--no-deps` 安装到 gbasis/scientific/fermi 三个 route；实际包路径均来自各自环境，NumPy 版本保持 `1.26.4`/`2.2.6`/`1.26.4`。
- `tests.test_prepare_runtime` 6 项 Passed；配置三个真实 route 的 `tests.test_processor_operations` 9 项 Passed。
