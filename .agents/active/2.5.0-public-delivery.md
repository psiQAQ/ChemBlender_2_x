# ChemBlender 2.5.0 Public Delivery

## Authority

用户已批准实现 2.5.0 对外交付计划：Standard `uv tool`、协议级 API、GUI 诊断、完整中英双语新 SOP、旧 SOP 归档、本机 Blender 私有及真实 profile 全公开面验收、可发布制品和稳定提交。

## Current State

- Branch: `release/2.5.0`
- Base: `d8f0306`
- User-owned change: `.gitignore`，必须保留且不提交。
- Completed predecessor: `.agents/completed/physical-quantity-visualization-sop.md`
- Active plan: `.planning/2026-09-10-2.5.0-public-delivery/task_plan.md`

## Next Gate

升级并构建 2.5.0，完成私有 Blender 实装、当前 UI 截图和全部公开面回归，再进行受保护的 `user_default` 安装。

## Latest Evidence

- routed worker 已改为目标 Python 的 `-I -m chemblender_prepare.worker.runner`，不再注入主环境源码或 `site-packages`。
- 同一 `0.1.0` wheel 已用 `--no-deps` 安装到 gbasis/scientific/fermi 三个 route；实际包路径均来自各自环境，NumPy 版本保持 `1.26.4`/`2.2.6`/`1.26.4`。
- `tests.test_prepare_runtime` 6 项 Passed；配置三个真实 route 的 `tests.test_processor_operations` 9 项 Passed。
- Standard wheel 已在全新临时 `uv tool`/Python 3.12 环境安装 NumPy、RDKit、Gemmi，两个 launcher 均生成；CLI 为 PE console subsystem 3，GUI 为 Windows GUI subsystem 2。
- `capabilities` 明确未配置 provider/QCEngine/专业 route 不可用；`doctor` 在 Standard 完整时 Passed 并逐项 warning 可选缺失，Standard 缺包回归为 Failed。
- prepare/runtime/controller 专项 51 项 Passed、1 项有原因 skip；`uv build --no-cache` wheel/sdist Passed。PyPI 来源因尚未发布而 Not Run。
- 中英 README、用户 SOP、prepare CLI/GUI/API/route 文档与自包含离线 HTML 已生成；公开清单为 10 CLI、9 GUI、21 operation、22 reader、13 export。
- 旧 2.4 工作流保留稳定 URL 并明确归档，2026-09-09 scientific SOP 冻结为资格证据；文档专项 82 项 Passed。
- Edge headless 已以 1440x1000 实际加载中英离线 HTML 并分别生成截图，均 Passed；产品 UI 截图待新安装的 2.5.0 私有实例采集。
