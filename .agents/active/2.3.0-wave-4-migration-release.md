# ChemBlender 2.3.0 Wave 4 — Migration and Release Qualification

## State

In progress. Wave 3 passed its exact-head CI and independent review gates and
was merged to `main` by ordinary merge commit
`7d58d1cb77037e9bc05e137e1d283f6d79c5a037`. Legacy Migration Tasks 1–6
are complete and independently approved: detection, neutral extraction,
staged project conversion, transactional migration, unified legacy operator
routing and proven-dead path deletion are available.

## Goal

交付 `2.3.0-rc.1`，完成旧路径统一、迁移、CI、性能、文档和最终2.3.0发布门。

## Success Criteria

- 旧File/SMILES/PubChem/CIF/POSCAR UI调用统一Import Pipeline。
- Legacy `.blend`显式迁移、备份和rollback。
- CI分层，目标真实integration不得skip。
- 动态版本/artifact、prerelease/final release正确。
- artifact size、wheel inventory、licenses和性能门通过。
- 用户、插件、迁移、故障排查和格式能力文档完整。
- RC后无新范围。

## Constraints

- 不删除仍有调用者或未迁移fixture覆盖的legacy代码。
- Release只使用exact-tag成功artifact，不重建。

## Verification

完整CPython、optional integration、Blender build/install/lifecycle、migration fixtures、performance、artifact audit和read-only release dry-run。

## Task 5 Pre-cleanup Caller Inventory (2026-07-31)

This is the proof snapshot used before Task 6. Task 6 subsequently removed
the zero-caller parser/export entries recorded below; the completed-task
evidence under the Execution Cursor is authoritative for the current tree.

- `scaffold.py`: the visible legacy `File`, `SMILES`, `PubChem` controls use
  `MESH_OT_SCAFFOLD_BUILD.execute()`. File covers CIF/POSCAR/CONTCAR and now
  reaches Quick Import; SMILES/preset creates an `ImportRequest`; PubChem
  stages an owned SDF before the same Quick Import boundary. At that trusted
  host boundary, every preview/materialization rechecks current session
  ownership, owned root, same-token metadata, strict CID URL and actual source
  SHA-256, then attaches deterministic existing `ProvenanceRecord` evidence;
  generic canonical parameters remain reader-neutral. The retained
  compatibility tail at `read.read_Cryst()` and `read.read_MOL()` is
  unreachable from those three current UI choices and remains for Task 6.
- `crys_utils.py`: direct legacy scaffold writers are `chem.supercell`,
  `chem.add_unit_cell`, `chem.add_crys_scaffold`, `chem.add_coordpolyhedra`,
  `chem.add_dummy`, and `chem.duplicate_symmetry`. `CRYSTAL_PT_TOOLS` hides
  its old controls for a unified Structure View, and each direct writer
  rejects that view with the Apply Scientific Edits diagnostic.
- `read.py`: `read_MOL()` retains the compatibility-only direct
  `download_sdf_from_pubchem()` branch; `read_Cryst()` retains the old
  `read_cif()`/`read_poscar()` parser path. None is a Task 5 UI action.
- `output.py`: `xyz_block()` has one production caller,
  `ui/scientific_edit.py._write_xyz()`, as the scientific-edit derived-XYZ
  helper; MOL/SDF/CIF/POSCAR block helpers remain compatibility helpers. The
  visible `chem.molecule_output` and `chem.update_cif_from_mesh` IDs now
  forward to the unified export and scientific-edit roots.
- `mesh.py`: `scaffold_to_mol()` remains the legacy editing/optimization
  boundary at its existing callers; no Task 5 deletion or parser rewrite is
  authorized.

## Execution Cursor

- Goal ID: `W4-MIGRATION-RELEASE`.
- State: `in_progress`.
- Branch: `feat/2.3.0-wave4-migration-release`.
- Worktree: `.worktrees/2.3.0-wave4-migration-release`.
- Baseline: `7d58d1cb77037e9bc05e137e1d283f6d79c5a037`.
- Current plan:
  `docs/superpowers/plans/2026-07-23-chemblender-2.3.0-wave-4-docs-release.md`.
- Completed plan:
  `docs/superpowers/plans/2026-07-23-chemblender-2.3.0-wave-4-performance-ux.md`.
- Current task:
  `Documentation and Final Release Task 3 — independent specification and
  code-quality review`.
- Continuous execution goal:
  complete every remaining local Wave 4 task in plan order, with TDD,
  per-task review, focused commits and persistent checkpoints.
- Remaining local plan order:
  - Documentation and Final Release Tasks 3–5 through local RC readiness.
- Remote release gate:
  Docs Tasks 6–7 require an authorized RC tag/push, exact-tag CI,
  prerelease publication and feedback window. Do not infer that authority
  from this local execution goal.
- Prepared fixture source:
  branch `feat/2.3.0-wave4-legacy-fixtures`, HEAD
  `e26319dd7697e49373840e00dbfbb13270b5cc42`, ordered commits
  `ab29560a54557ca4bb794e9e8043cd2110abc10b` then
  `e26319dd7697e49373840e00dbfbb13270b5cc42`.
- Completed Wave 4 tasks:
  - `Legacy Migration Task 1 — hash-locked legacy fixtures`;
  - prepared commits `ab29560a54557ca4bb794e9e8043cd2110abc10b` and
    `e26319dd7697e49373840e00dbfbb13270b5cc42`;
  - integrated commits `63b298ad39f374d3e19777b62d01f3e96b8a0b13` and
    `69665c329395449e468f23fa7b954a9b58df4f08`;
  - inventory 3 passed; full suite 1811 passed / 26 skipped / 0 failed;
  - documentation contracts 16 passed; `compileall` and
    `git diff --check` passed;
  - Blender 5.1.2 independently reopened all three fixtures with zero linked
    libraries and zero external file images.
  - `Legacy Migration Task 2 — non-mutating detection and extraction`;
  - implementation `85870573586f196f3860c2141b97a829721287f1`;
  - review fix `9ed363c0ace00b089cc11af56f13a5c1565ac544`;
  - final-review fix `10c561df95be6171858e373063354dd460eebbe2`;
  - focused detection/inventory/documentation 24 passed; full suite
    1816 passed / 26 skipped / 0 failed;
  - Blender 5.1.2 fixture/current/mixed/nonuniform-transform contracts,
    outside-Blender import, extension validate, `compileall`, and
    `git diff --check` passed.
  - `Legacy Migration Task 3 — migration preview and project conversion`;
  - implementation `f848a8f9b820fe74e8c3c036700d31e3f1706408`;
  - review fixes `d6e5283ec1b58c1ff9a17fe46899113cebbdae97`,
    `576f6e0f3acc85ee08f1d885b32276fd072f933d`, and
    `2837a03874181d867c2f24438625f7bd1a3b5ae7`;
  - frozen `LegacyMigrationPlan`, content-bound base/candidate validation,
    extraction-time `.blend` hash proof, unified molecule/crystal conversion,
    canonical topology/display alignment, and verified sidecar adoption;
  - focused verification 67 passed; full suite 1842 passed / 26 skipped /
    0 failed;
  - Blender 5.1.2 fixture extraction, extension validate/build, ZIP
    CRC/path/inventory audit, outside-Blender import, `compileall`, and
    `git diff --check` passed; independent closure review clean.
  - `Legacy Migration Task 4 — Blender migration wizard and rollback`;
  - implementation `2f819d9fc9ec859973d3a618b6cfe545e3a4e478`;
  - review fixes `171cbdf09c02fb5898ee3e9b97128dd9a53b44ee`,
    `a91fac3247e684d131dfe3cb2f99f8696437bc31`,
    `3a22879a4df5d53957e482ca86d3f251405bb105`,
    `9d6768952b36cb351404f6d58239b6ce31ea5e52`, and
    `00b07f9a72c9710699f69e90b81471869f9fda80`;
  - explicit preview/confirmation, candidate-only entity inventory,
    backup-only diagnostics, same-parent sidecar swap, paired `v2` Backup
    ownership and exception-preserving rollback;
  - controller focused verification 54 passed; full suite 1843 passed /
    26 skipped / 0 failed;
  - Blender 5.1.2 migrated and reopened all three hash-locked fixtures,
    including post-mutation rollback, unloaded lazy arrays, exact entity/view
    bindings, display/material/node settings and per-view-layer hide state;
  - native validate/build, 176-entry ZIP audit, isolated installed-extension
    lifecycle, `compileall` and `git diff --check` passed; final independent
    review Approved with zero findings.
  - `Legacy Migration Task 5 — route migrated legacy UI actions to the unified backend`;
  - visible File/SMILES/PubChem controls now use shared ImportRequest/Quick
  Import, session-owned PubChem SDF staging is reverified at the host boundary
  before deterministic provenance attachment and persists URL/SHA-256 through
  the existing ProvenanceRecord;
    unified Crystal views hide/reject legacy direct writes, and old
    export/scientific-edit IDs forward to their unified roots without stale
    export properties;
  - implementation `d107476`, review fixes `1bba0c9`, `992a68f`, and
    `2f4dc86`; the final host attachment runs once outside reader fallback,
    preserves reader-neutral identities, rejects scientific-batch mutation,
    and cleans lazy arrays, memmaps and reader artifacts on failure;
  - final focused routing/attachment/deferred/caller-map evidence passed,
    including 74 focused contracts before the final lifecycle hardening;
    `compileall`, native extension validate, and isolated installed-extension
    lifecycle smoke passed (Blender 5.1.2, 244.2 s); independent review
    Approved with zero findings.
- `Legacy Migration Task 6 — remove retired legacy paths after caller coverage is proven`;
  - commits `36164c2` (molecular parser/scaffold path), `c9bba01`
    (crystal parser path; supersedes the unpushed `2280b3a` EOF-only amend),
    and `f69288f` (dead MOL/SDF/CIF/POSCAR block exporters);
  - AST caller map and source search prove zero production callers for
    `read_MOL`, `read_Cryst`, `read_cif`, `read_poscar`,
    `mol_block_v2000`, `mol_block_v3000`, `sdf_block`, `cif_block`, and
    `vasp_block`; retain only migration/direct-crystal compatibility helpers
    and the sole Scientific Edit `xyz_block` caller for the 2.4 boundary;
  - follow-up review coverage restores the active SDF, MOL2, SMILES, and three
    preset operator routes as real `MESH_OT_SCAFFOLD_BUILD.execute()` contracts:
    each creates an `ImportRequest`, dispatches to its unified Quick Import
    boundary, and does not load the retired legacy reader module; the removed
    parser-only tests remain retired;
  - focused routing/docs verification 37 passed; full suite 1862 passed /
    26 skipped / 0 failed with the active Blender extension shared dependency
    path; `ChemBlender/_math.py` is UTF-8 without BOM and entirely CRLF, with
    the Task 6 base-to-tip diff containing only the intended function deletions;
    `compileall`, Blender 5.1.2 native validate/build, 178-entry ZIP
    CRC/path/wheel audit, and fresh short-path isolated `user_default`
    lifecycle smoke passed (242.9 s); long temporary sidecar paths remain the
    documented Windows baseline and are not a Task 6 code regression.
  - review-fix commit `9c65d12888372f06b69a99433ec93213e3507b76`;
    final independent review Approved with zero findings.
- `CI, Dependency and Release Hardening Task 1 — dependency inventory`;
  - commits `b80e6df`, `67e7d92`, `804bf3a`, and `f11e9aa`;
  - canonical bundled-wheel inventory covers pinned RDKit and Gemmi filenames,
    URLs, hashes, ABI/platform, SPDX/license members and exact size ceilings;
  - stdlib CLI validates exact schema, manifest equality, resolved containment,
    ZIP paths/member types/licenses, and produces deterministic paired JSON
    outputs with recoverable atomic rollback;
  - real RDKit/Gemmi wheels, 32 focused dependency/release tests, 34
    documentation/repository tests, `compileall` and diff checks passed;
    independent review Approved.
- `CI, Dependency and Release Hardening Task 2 — split native core CI from package CI`;
  - `extension-package.yml` now starts with a 10-minute `native-core` job
    using only checkout, `actions/setup-python`, explicit standard-library
    dependency-inventory/fixture/docs/repository workflow contracts,
    `compileall`, and `git diff --check`;
  - the 30-minute `package` job explicitly `needs: native-core`, retains the
    approved wheel and Blender downloads, full Blender test/build/isolated
    cold-install lifecycle, and is the only job that uploads the package
    artifact;
  - RED proved the old single `build` job had no `native-core`/`package`
    boundary and only one timeout; GREEN workflow/release contracts passed
    101 tests with one expected Windows symlink-privilege skip;
  - review fix: native checkout now fetches full history and checks the
    committed event range (`pull_request.base.sha` or `push.before`), with
    40-hex/nonzero validation and `HEAD^`/root fallback before
    `git diff --check <base> HEAD`; a temporary-repository contract proves a
    committed trailing-whitespace head fails while a clean head passes;
  - review fix: the shared stdlib range checker now fetches a declared missing
    event SHA and verifies the exact commit or fails closed, while zero/empty
    event bases use the default-branch merge base or a full empty-tree diff;
    deleted pushes skip `native-core` and thereby the dependent package
    artifact;
  - review fix: the Windows PowerShell invocation now passes every nullable
    helper value as `--flag="$env:VAR"`; real workflow-command tests cover
    push, pull-request, and workflow-dispatch null bases and prove the checker
    receives an empty value instead of exiting from a missing `argparse` value;
  - Remote CI Not Run by authorization boundary.
- `CI, Dependency and Release Hardening Task 3 — add optional quantum-core integration CI`;
  - completed locally: read-only `.github/workflows/optional-qc-core.yml` uses
    isolated cclib and IOData CPython 3.13 jobs plus GBasis CPython
    3.12/NumPy 1.26.4, with exact package versions, exact cclib/IOData/GBasis
    gitlinks, selected fixture SHA-256 preflight and full SHA-pinned actions;
  - `ChemBlender/scripts/run_required_integration.py` writes atomic canonical
    JSON summaries containing test IDs, counts, runtime/package versions and
    fixture hashes; targeted skips, zero discovery, load error, fixture/version
    preflight failure, failure and error return nonzero before they can be
    reported as optional success;
  - focused runner/workflow/native/repository/docs contracts passed (48 tests);
    full controlled Blender Python suite passed (1901 tests, 26 skips) with
    the existing extension shared site-packages providing Gemmi 0.7.5 and
    RDKit 2026.03.3; Remote CI Not Run by authorization boundary.
  - review-fix: the required runner now records stable subtest failure/error
    IDs, expected failures and unexpected successes with explicit counts, and
    passes only when every run test is an ordinary success; each backend now
    has an exact direct-plus-transitive runtime lock under
    `.github/constraints/`, shared by `pip -c` and metadata verification.
  - review-fix verification: RED exposed the former false-success cases;
    runner/workflow/native/repository/docs/dependency contracts passed 70
    tests, controlled full discovery passed 1907 tests / 26 skips, and all
    three cross-platform `pip --dry-run --report` lock resolutions matched
    their committed files. No local package installation or Remote CI run.
- `CI, Dependency and Release Hardening Task 4 — enforce artifact size and license budgets`;
  - completed in `4bac537`, `ac55ec9`, `5d0a589`, and `070ada4`; added
    standard-library `artifact_size_report.py`, versioned
    `.github/artifact-budgets.json`, deterministic malicious-ZIP fixtures, and
    package/release workflow contracts; outer and nested ZIP paths reuse the
    Task 1 validator and reports are canonical/atomic with no disk extraction;
  - CI now emits one verified package artifact containing the ZIP, checksum,
    `wheel-inventory.json`, `wheel-license-copy-list.json`, and
    `artifact-size.json`; explicit `package-ci` re-verifies all metadata while
    explicit `release-assets` remains ZIP/checksum only while requiring the
    tagged-source outer ZIP budget before CRC or member reads;
  - fresh Blender 5.1.2 build measured package SHA-256
    `605a2027adf7bd58af91b349150fffb7d8e11444b532448d5b794dedc7b3cc7e`
    at 29,936,732 bytes. This replaces the ignored 29,936,106-byte local
    measurement with zero allowed unexplained growth; nested RDKit/Gemmi hash,
    size, SPDX and license evidence passed. Focused contracts: 76/76 passed;
    controlled full Blender-Python discovery: 1,917 tests / `OK` / 26 explicit
    skips using the CI-equivalent shared wheel site. Review hardening adds
    zero-growth outer/member/section unpacked budgets checked from validated
    ZIP central-directory metadata before any content read; the fresh artifact
    passed both `package-ci` and `release-assets` verification. Final
    independent review: Approved. Remote CI Not Run by authorization boundary.
    Detailed evidence:
    `.agents/completed/2.3.0-wave4-ci-task4-report.md`.
- `CI, Dependency and Release Hardening Task 5 — harden prerelease/final workflow and exact-run selection`;
  - commit `b61a634`; strict standard-library JSON selectors now fail closed unless
    GitHub returns exactly one successful exact-SHA tag package run and exactly
    one unexpired metadata-named artifact, while the workflow retains all GitHub
    data acquisition and publication behavior;
  - verify and publish first re-check the complete five-file Task 4 package
    artifact in `package-ci` mode against tagged dependency/budget sources, then
    re-check the ZIP/checksum-only public assets in `release-assets` mode;
  - release notes now require root and tagged `CHANGELOG.md` entries to match;
    the existing annotated-tag/origin-main proof, draft/digest/publish order,
    prerelease non-latest/final latest behavior, and existing-Release refusal
    remain fail-closed;
  - RED: missing selector interfaces/CLI and absent complete-artifact
    re-verification; GREEN: 77 focused release/artifact/repository/native/docs
    contracts plus `compileall` passed; `git diff --check` passed; Remote
    CI/dispatch Not Run by authorization boundary.
  - review fix round 1/5 in `ad40843`: `gh api --paginate --slurp` now obtains
    every server-filtered exact-tag workflow-run and selected-run artifact page;
    the standard-library helper flattens all workflow-run pages before exact-one
    selection, and its selected `artifact_id` is used to download the artifact
    bundle. Verify and publish whitelist exactly five outer-bundle members before
    any extraction, rejecting extra, duplicate, directory, or traversal members;
    RED exposed the bounded `gh run list` path and missing pagination interfaces;
    GREEN: 79 focused release/artifact/repository/native/docs contracts plus
    `compileall` and `git diff --check` passed. Remote CI/dispatch Not Run by
    authorization boundary.
  - review fix round 2/5 in `776631e`: the exact-run selector now strictly
    consumes the REST `id`, `head_sha`, and `head_branch` fields, emits
    `run_id`, and the workflow passes that value to the artifact REST endpoint;
    old `gh run list` camelCase records remain unsupported. RED: a real REST
    page fixture failed on the old missing `databaseId` field and the workflow
    contract failed while requesting `.database_id`; GREEN: 79 focused
    release/artifact/repository/native/docs contracts plus `compileall` and
    `git diff --check` passed. Remote CI/dispatch Not Run by authorization
    boundary.
  - Final independent review: Approved. The final review suite passed 105
    tests at HEAD `6be85c834ea76b9ae382e0b344f3dc4767d0b2d2`; Task 6 remains
    next. Remote CI/dispatch Not Run by authorization boundary.
- `CI, Dependency and Release Hardening Task 6 — document branch protection and required checks`;
  - commit `5769fe7`; added the exact current `native-core`, `package`,
    `cclib`, `iodata`, `gbasis`, and release-dry-run check contexts with
    triggers, ownership, optional-backend change rules, and the explicit
    not-applied branch-protection recommendation;
  - RED confirmed the required-check document was absent; GREEN verified live
    workflow job names, UTF-8/no-BOM/CRLF, and links. Focused documentation,
    repository, native-core, and optional-workflow contracts: 45 passed;
    `git diff --check` passed. Remote settings/CI/dispatch Not Run by
    authorization boundary.
  - Final independent review: Approved; the 45 focused docs/repository/workflow
    contracts and required-check document contract were re-run at checkpoint.
- `Performance and UX Hardening Task 1 — unified benchmark harness and datasets`;
  - implementation `51bfbc9` adds deterministic 50k/250k structure,
    1k/100k lazy trajectory, 128³/256³ grid and 10k/100k SDF-index fixture
    generators, plus canonical atomic JSON benchmark reporting;
  - fixed case registry covers enable, preflight, parse, commit, sidecar,
    VDB, default view, trajectory, browser and cancellation stages; only
    existing pure-core runners execute in CPython, while Blender-only and
    future cancellation stages report explicit boundaries rather than false
    results;
  - RED proved the missing package/script contract; GREEN passed 7 focused
    harness contracts and the controlled 157-test related benchmark, sidecar,
    reader, browser, documentation and repository collection with existing
    extension shared `rdkit 2026.03.3` and `gemmi 0.7.5`; no dependency was
    installed and no full-scale benchmark was run.
  - review fix `4044223` moves source/hash, lazy trajectory and parsed-batch
    preparation outside the timer exactly once per selected scale; fresh
    project/sidecar/browser setup and sidecar cleanup stay outside each sample
    timer, while the first fixture access is cold and at least two later
    samples define hot statistics. RED exposed repeated fixture generation
    and one-sample false qualification; focused 10 tests and the controlled
    160-test related collection passed with the same existing shared wheels.
  - review fix `18b48d9` gives `_prepare_fixtures()` its own failure cleanup:
    a successfully created lazy trajectory closes exactly once if a later
    source/batch setup operation fails, and any cleanup failure is added to
    the primary error rather than replacing it. RED/GREEN focused harness
    evidence is 11 tests; `compileall` and diff checks passed.
  - final completion commits: `51bfbc9`, `4044223`, and `18b48d9`; final
    focused harness verification is 11/11 and the controlled existing-wheel
    benchmark/sidecar/reader/browser/documentation/repository collection is
    160 passed. Final independent review: Approved.
- `Performance and UX Hardening Task 2 — reference baseline and regression policy`;
  - implementation `bbb58f3`, review correction `d980263`, and exact-evidence
    checkpoint `e35595b`; versioned
    `ChemBlender/benchmarks/budget.json` schema 2.0 maps every approved local
    SLA to `p95_seconds`: enable 2.0 s, first feedback 0.5 s, default view
    3.0 s, Cube cache/view 10.0 s, cached frame 0.1 s and browser filter
    0.2 s. The five cold gates require repeated `cold_p95`; cached trajectory
    requires `hot_p95`;
  - the Task 2 trend review policy is p95 +15%, with fail-closed requirements
    for all six product cases, matching scale/cache state, clean source, and
    compatible CPU/platform/Python/Blender/RDKit/Gemmi environment; it is
    explicitly new review policy, not a claimed historical approval;
  - the fresh clean-source `d980263` Windows 10 / Blender 5.1.2 observation
    used Ryzen 7 4800H, 16 GiB RAM and runtime-imported RDKit 2026.03.3/Gemmi
    0.7.5. Its tracked raw canonical JSON at
    `docs/quantum-visualization/2.3.0/benchmarks/2.3.0-rc-reference.json`
    records `source_dirty: false`;
  - every current Task 1 result is `diagnostic`, not product evidence: it
    lacks Import Preview summary, real trajectory-manager/Blender update, or
    10,000-record SDF Browser coverage. The observed p95 values (preflight
    0.253977 s, frame slice 0.000007 s, browser one-structure filter
    0.000023 s) are diagnostics only; no local hard gate is marked Passed.
    Parse, project commit and sidecar save/open are also recorded in
    `docs/quantum-visualization/2.3.0/benchmarks/2.3.0-rc-reference.md`;
  - all six product gates are `Not Run`: the preflight, trajectory and Browser
    measurements are diagnostic-only, while `extension_enable`, `vdb_cache`,
    `default_view` and future `cancel_cleanup` are explicit raw `Not Run`
    cases. The full report exits nonzero as designed, and diagnostic cases are
    rejected by the sole budget comparison entrypoint; no complete local SLA
    qualification is claimed;
  - RED added cold/hot measurement, diagnostic-only, runtime-version, clean
    provenance and floating-boundary contracts. Green focused
    harness/budget/documentation/repository verification passed 54 tests,
    together with `compileall ChemBlender`, raw-JSON canonical/provenance
    verification and `git diff --check`; product runner implementation and
    clean user-resource product qualification remain scheduled work. Final
    independent review: Approved.
- `Performance and UX Hardening Task 3 — remove UI-thread blocking in import and cache flows`;
  - completion commits: `4ab7242` (cancellable UI task state machine),
    `815ff3f` (atomic cancellation handoff), and `2af9fee` (Volume terminal
    state import);
  - controlled shared-site-package contracts: 244 passed; final independent
    review suite: 151 passed;
  - Blender 5.1.2 native extension validation/build and isolated
    `BLENDER_USER_RESOURCES` lifecycle, Volume modal-success, and unload
    smoke passed.
  - Final independent review: Approved.
- `Performance and UX Hardening Task 4 — Optimize Project Browser projection and search`;
  - implementation `c498f1b`; the first independent review was Not Approved
    because paging ignored non-record row overhead, project replacement could
    reuse an old index, large search omitted current ViewRecord fields, broad
    queries rebuilt/sorted too much data, and the index cache had no bounded
    lifecycle;
  - fix round 1: this commit. Paging now starts from total indexed and
    estimated ancestor/detail/view cost, so 997 records plus Structure,
    Topology and Grid remain within the 1000-row RNA limit. Projects without
    molecular records use the same generic result-page navigation;
  - both Browser modes are pre-sorted once in a lightweight index and broad
    search/filter scans that order once while retaining only the requested
    page. Current-call ViewRecord label/kind/quality participate dynamically
    without entering the index, and matching entities retain source/revision
    ancestors plus all sibling views;
  - revision-keyed row caching and a project/session structural-count index
    avoid rebuilding the 10k/100k index for pure browser-revision changes.
    Runtime project identity prevents replacement reuse; the index LRU is
    limited to two and has per-session/global clear APIs wired to session
    cleanup and unregister. Unreliable direct calls persist neither cache,
    and no cache retains projects, entities, or lazy scientific arrays;
  - migration success advances the Project Browser revision exactly once;
    rollback does not advance it;
  - independent 100k, nine-sample warm measurements: broad By Source
    median/p95 0.145407/0.146295 s, “Molecular Records” median/p95
    0.141086/0.142120 s, and exact last-record median/p95
    0.100334/0.102481 s; all remained under the 0.2 s product budget and
    left the lazy Grid unloaded;
  - focused Project Browser/contracts: 96 passed; final complete controlled
    Blender-Python suite: 1980 passed / 26 skipped / 0 failed. Blender 5.1.2
    native preflight/validate/build and ZIP audit passed; built ZIP:
    29,947,343 bytes, SHA-256
    `59a363675475e637b0eabad0a53729f6194cdcbf951a7e993f8e418c9c92ad68`;
  - fail-closed isolated smoke first exposed that active-worker validation
    re-enabled an extension after binary RDKit/Gemmi use, racing Blender's
    Windows wheel reconciliation and corrupting the same-process probe. RED
    reproduced twice in independent profiles. The smoke now keeps the earlier
    two reload cycles, probes RDKit/Gemmi before final unload, validates active
    worker cancellation during that single final unload, and never re-enables
    afterward. A fresh `D:\cbt4fc3` run finished with
    `PASS: ChemBlender extension lifecycle` and exit 0, including the real
    10,000-record SDF workflow, exact additional-entry count, bounded 1000-row
    RNA projection, Structure/Topology search and lazy Grid remaining
    unloaded.
  - independent review fix round 2: this commit. Project Browser unregister
    now clears caches early but retains session cleanup until every owned RNA
    property has been removed; a partial `delattr` failure remains retryable,
    and register repairs the missing half without losing or duplicating the
    cleanup callback;
  - large view-only search and quality filters now emit only matching views,
    while an entity match still emits every sibling view. The streaming scan
    constructs the visible-view map during page consumption without a second
    scan, and 10/1000-record parity tests lock the existing small-project
    semantics;
  - source-only and mixed projects now index standalone source and uncovered
    source/revision branches in the same lightweight pager. More than 1000
    branches remain bounded and searchable without eager BrowserRow tree
    materialization; the page-size label is `Entries per Page`;
  - independent 100k, nine-sample warm measurements after round 2: broad By
    Source median/p95 0.160471/0.163384 s, “Molecular Records” median/p95
    0.130194/0.130957 s, and exact last-record median/p95
    0.025892/0.026280 s; all remained under the 0.2 s product budget and left
    the lazy Grid unloaded;
  - round-2 focused Project Browser/contracts: 101 passed; final complete
    controlled Blender-Python suite: 1985 passed / 26 skipped / 0 failed.
    Blender 5.1.2 live MCP preflight, native validate/build and 183-entry ZIP
    audit passed; built ZIP: 29,947,819 bytes, SHA-256
    `e8318f40e59c69d2d8b4b3c17f88a916b4036b5f70debd53868413d89873e604`;
  - a fresh fail-closed `D:\cbt4r2` run finished with
    `PASS: ChemBlender extension lifecycle` and exit 0, including the real
    10,000-record SDF workflow, exact 40,001 additional entries, bounded
    1000-row RNA projection, dynamic Browser filtering, and lazy Grid
    remaining unloaded.
  - independent review fix round 3: this commit. Small-project view rows now
    retain `ViewRecord.view_kind` in a trailing compatible BrowserRow field,
    and `_filtered()` includes it in search text. Large and small projections
    therefore share view-kind search semantics without changing view row kind,
    eager materialization, or sibling inclusion rules;
  - RED parity reproduced `search="isosurface"` returning no result for 10
    records while 1000 records returned the matching molecular record and
    only `Rare surface label`. GREEN locks identical 10/1000 results and
    confirms the nonmatching sibling remains excluded;
  - round-3 Project Browser plus repository/docs/workspace suite: 136 passed;
    `compileall` passed. An independent 100k single warm sample measured broad
    source 0.137745 s, broad records 0.132238 s, and exact last-record
    0.026177 s; all remained under 0.2 s and left the lazy Grid unloaded;
  - the complete 1985-pass suite, Blender 5.1.2 native validate/build,
    183-entry ZIP audit, and fresh fail-closed lifecycle from `af5228b` were
    not re-run because round 3 changes only pure BrowserRow search metadata
    and its Python regression test.
  - final independent review checkpoint: Approved at reviewed HEAD
    `255085f2ec4203f36ff0ff10002831e958438f23`; independent suite 137/137
    passed. Independent 100k p95 measurements were By Source 0.1696 s,
    Molecular Records 0.1609 s, and exact last-record 0.0260 s. The final
    review reported no Critical, Important, or Minor findings.
- `Performance and UX Hardening Task 5 — Improve quality, revision and recovery UX`;
  - implementation `51717d6`; fixed-HEAD independent
    specification and code-quality review found seven Task 5 issues;
  - one shared `ui.diagnostics` boundary now owns the five quality badges,
    canonical diagnostic detail/copy/export, exact revision action choices
    and status-specific project-link recovery actions;
  - a `NEW_REVISION` commit never creates or switches a default View. It
    persists exact current/new revision IDs, then requires
    `Update Selected Views`, `Comparison View` or
    `Keep Current`; failures roll back new View objects and old visibility;
  - Relink and Verify reuse the atomic multi-Scene services and revalidate
    live `session.link_status` inside `execute()`. Relink is a `.cbq`
    `FILE_PATH`; inspection-only does not adopt/write a candidate or claim
    global write protection; Detach atomically removes only project-link
    metadata and preserves existing Blender objects;
  - RED separately proved the missing quality/registration root, automatic
    new-revision View creation, absent exact revision prompts and absent
    canonical report persistence. GREEN focused verification passed 201
    tests; final complete controlled Blender-Python discovery passed 2004 /
    26 skipped / 0 failed; `compileall` and `git diff --check` passed;
  - Blender 5.1.2 live MCP preflight, native validate/build and a 184-member
    safe-path/type/duplicate/CRC/manifest/wheel ZIP audit passed. Package:
    29,953,952 bytes, SHA-256
    `acd9fc582f773697a0b34da94ab8a1745ab207509805798fce6a1811788b48b0`;
  - the strict release-size report correctly rejected growth above its
    pre-Task-5 zero-growth baseline. Task 5 did not weaken or rebase that
    release budget; final qualification must review the intentional code
    growth before claiming the artifact-size gate;
  - two initial background smoke attempts exposed Blender-specific test
    oracle behavior: background clipboard writes cannot be read back, and a
    scripted `bpy.ops` ERROR report raises `RuntimeError`. The smoke now
    verifies clipboard bytes only in a foreground window and asserts the live
    recovery rejection exception. Production behavior was unchanged;
  - fresh short-profile `D:\cbt5s3` product/lifecycle smoke passed in Blender
    5.1.2 (245.9 s) with the explicit PASS marker, including Task 5 operators,
    the 10,000-record SDF workflow, save/reopen and reload/unload. Existing
    Windows loaded-wheel cleanup warnings remain non-functional baseline
    noise.
  - independent review fix round 1: `3eef4f7`. Diagnostic preview now
    traverses and renders bounded data while canonical copy/export remains
    complete; diagnostic pages are reachable through a registered operator;
    revision prompts persist unresolved pairs, derive replacements from live
    preset bindings, group multi-object Views by render identity, validate the
    original normalized settings and fail closed on stale or ambiguous
    metadata. Update/Comparison only affect the intended logical View.
  - project-link recovery now names inspection honestly, preserves fatal
    detach/rollback exceptions, and clears only link/cache retry reasons after
    a successful detach so the following clean save is a filesystem no-op.
  - review-fix RED: the initial 132-test focused run failed in the expected
    diagnostic paging/bounding, prompt persistence, revision grouping and
    recovery cases; a real signed-isosurface smoke additionally exposed
    derived `negative_isovalue` replay as invalid user input.
  - review-fix GREEN: 231 focused tests passed; final controlled
    Blender-Python discovery passed 2013 / 26 skipped / 0 failed;
    `compileall` and `git diff --check` passed. Blender 5.1.2 native
    validate/build and a 184-member ZIP audit passed. Final package:
    29,955,650 bytes, SHA-256
    `0db367c18fd849897bb5ca0189c50c573bda40ea5db35c565a99f882115356ec`.
    The artifact-size gate remains Failed at +18,918 bytes; its budget was not
    changed, and final qualification must intentionally review and rebaseline
    the Task 5 code growth.
    Fresh isolated `D:\cbt5r1i` product/lifecycle smoke passed in 248.0 s
    with `--python-exit-code 1` and the explicit PASS marker, including real
    two-object signed-surface Update and Comparison actions. Existing loaded
    wheel cleanup warnings remain non-functional baseline noise.
  - documentation fixes `7e5fbbd`, `8dbae6b`, and `51f6fdf` align the
    architecture guide with Inspect Existing, live revision mapping and the
    bounded diagnostic paging/full canonical export boundary.
  - final specification and code-quality reviews: Approved at exact HEAD
    `51f6fdfe10eaf9a1ca269ae424977fd4bce4f9c3`; no Critical, Important or
    Task-related Minor findings remain. Task 5 is completed.
- `Performance and UX Hardening Task 6 — Conduct scripted usability acceptance`;
  - initial implementation `fff5cebdac7c101f2e63497c72d3a4405028784e`;
  - repeatable script:
    `docs/quantum-visualization/2.3.0/usability-test-script.md`; measured
    results: `docs/quantum-visualization/2.3.0/usability-results-rc1.md`;
  - the first independent specification and code-quality reviews were Not
    Approved because the initial evidence overstated hybrid service/core
    assertions, legacy acceptance imported the checkout instead of installing
    the package, and the runbook did not fail closed on reused profiles,
    process exits or missing PASS markers;
  - review-fix round 1
    `70467b4a49a5120465de712ad307c880256a0032`: the runbook now creates
    unique short `user`/`tmp` profiles and refuses reuse, checks validate/build
    and each waiting Blender process, requires explicit PASS markers, preserves
    final raw stdout/stderr/timing, and documents the exact operator versus
    installed UI service/core/`ExportJob` layer for U01–U12;
  - `tests/blender_usability_legacy.py` installs the exact ZIP into a separate
    fresh `user_default` repository for every hash-locked fixture, verifies the
    enabled module is exactly `bl_ext.user_default.chemblender`, calls the real
    preview/migrate operators, and verifies save/reopen plus the connected
    sidecar without importing the checkout package;
  - final fresh extension-native Blender 5.1.2 product smoke used
    `D:\cbu-product-3d7aa3a8`, exited `0` after 253.574215 s and printed
    `PASS: ChemBlender extension lifecycle`; XYZ/SDF/Cube/CIF/PDB,
    diagnostics, Cube semantic resolution, conformer grouping, save/reopen,
    revision actions, scientific edit and export assertions all passed;
  - the final 10k SDF scenario measured 232.041566 s wall time and
    126,467,983 bytes peak memory; browser filter median/p95 was
    21.0861/21.0909 ms and the bounded 1,000-row RNA projection was
    5.73/5.79 ms;
  - each hash-locked legacy fixture was migrated and reopened through the
    packaged Extension in a separate fresh Blender process: 2.1 molecule
    8.118509 s, 2.2 crystal 8.107171 s and 2.2 edited scaffold 8.083491 s;
    all three verified fixture/package hashes, printed
    `PASS: packaged legacy migration and reopen`, and exited `0`;
  - review-fix verification: 73 focused tests passed; the first full invocation
    intentionally exposed a missing optional-dependency environment
    (`GemmiDependencyError`), then Gemmi 0.7.5/RDKit 2026.03.3 were probed in
    the existing approved shared dependency path and the unchanged suite passed
    2014 tests / 26 skipped / 0 failed; `compileall` and `git diff --check`
    passed;
  - the specification re-review Approved round 1; quality re-review closed all
    original findings and found one scoped Minor: packaged U12 did not yet
    assert every original object's backup ownership and transaction markers;
  - review-fix round 2
    `1ca17a5a4fc45ac3f244a28707ada94875c29c11`: the packaged harness now
    verifies after migration and again after reopen that every detected
    original object still exists, belongs only to
    `ChemBlender Legacy Backup`, carries backup contract `v2`, and has
    project/transaction markers equal to the backup collection; all three
    exact-hash fixtures passed again in new fresh profiles at 8.124480 s,
    9.107583 s and 8.137975 s with exit `0` and the exact PASS marker. Product
    smoke and the 2014-test full suite were not rerun because this commit only
    strengthens the external acceptance assertion; their unchanged evidence
    remains from `70467b4a49a5120465de712ad307c880256a0032`;
  - issue classification: 0 Blocker, 0 Major, 3 Minor (existing syntax/test
    data/native-DLL cleanup warnings); no runtime fix or scope expansion was
    made. Remote CI Not Run;
  - final independent specification and code-quality reviews: Approved at
    exact HEAD `1ca17a5a4fc45ac3f244a28707ada94875c29c11`, with zero
    findings. Task 6 is completed.
- `Documentation and Final Release Task 1 — Replace the old product
  description and add user guides`;
  - implementation
    `80d3ad1549ccd0288a1442c1dce7f25d4c9a2c29`;
  - independent-review handoff
    `4fa3ce20a221b9c708bb41d31848cc6b907a47b4`;
  - RED:
    `python -m unittest
    tests.test_quantum_visualization_docs.QuantumVisualizationDocsTests.test_user_guides_describe_verified_product_workflows
    -v` exited `1` because the old root README did not contain the required
    result-first product position;
  - GREEN: 46 documentation, Reader API, repository and manifest-validation
    contracts passed; `compileall -q tests`, cached/working
    `git diff --check`, local-link resolution and UTF-8/no-BOM checks passed;
  - root README, manifest tagline, six `docs/user/` workflow guides and the
    documentation index now describe the verified platform, base format,
    Quick Import/Preview, Project Browser, quality, sidecar/recovery,
    scientific-edit and topology boundaries without changing runtime,
    version, changelog or release metadata;
  - the initial specification review Approved; the initial code-quality
    review found three scoped documentation issues: QCSchema was omitted from
    the formal built-in reader scope, Maximum validation was described as a
    global retention promise rather than a reader/format-specific contract,
    and the guide incorrectly said the current topology controls display the
    topology revision;
  - review fix
    `1c5a4a2e5cdf3b302cedc171d560d6491557df7d` adds the dependency-free
    QCSchema Molecule/AtomicResult and raw-envelope boundary, corrects both
    validation guides, and distinguishes stored binding/provenance revision
    identity from current control visibility;
  - review-fix RED: the strengthened documentation contract exited `1`
    against the old guides because the root README omitted QCSchema;
    review-fix GREEN: 58 documentation, Reader API, repository,
    manifest-validation, QCSchema and reader-catalog tests passed;
    `compileall`, local-link/UTF-8/no-BOM contracts and cached/working
    `git diff --check` passed;
  - final independent specification and code-quality reviews Approved at
    exact HEAD `1c5a4a2e5cdf3b302cedc171d560d6491557df7d`
    (specification 55/55, quality 30/30), with zero findings. Task 1 is
    completed.
- `Documentation and Final Release Task 2 — Publish machine-derived format
  and dependency documentation` is completed;
  - implementation
    `10dd8a8ca93178a9a9e9c4faba9631bbd98b4207`;
  - initial review handoff
    `e5d0d6637ca4d64c078af06a9145dd1f2880f67e`;
  - RED:
    `python -m unittest tests.test_generated_docs_fresh -v` exited `1`
    with four expected failures because the generator, canonical JSON files
    and marked Markdown section did not exist; a later line-ending regression
    test separately failed because the first generator draft converted the
    tracked CRLF user guide to LF;
  - GREEN: 139 generated-doc, reader descriptor/API/conformance, POSCAR,
    dependency-inventory, documentation, repository and extension-validation
    contracts passed; `compileall -q ChemBlender tests`,
    `generate_format_docs.py --check` and `git diff --check` passed;
  - two consecutive generator runs produced byte-identical canonical format
    JSON, dependency JSON, compatibility matrix and marked Markdown. The
    generated user guide retained UTF-8/no-BOM CRLF, both JSON documents are
    canonical UTF-8 with one LF, and the seven documented Project Browser
    export IDs matched the existing `_FORMAT_ITEMS` registry in a read-only
    AST audit;
  - the capability document remains runtime-neutral: it records probe
    contracts rather than a current `available=true` result. Bundled
    dependency facts come only from validated `dependencies.toml`; no wheel
    was read, downloaded or installed;
  - the initial specification and code-quality reviews were Not Approved at
    exact HEAD `e5d0d6637ca4d64c078af06a9145dd1f2880f67e`. Specification found
    the XYZ export overstated as F5/lossless and missing exact
    extensionless ASE/pymatgen basenames. Quality found noncanonical
    generated-section markers were not uniformly rejected, the Project
    Browser export-ID audit was not automated, and fixture-family metadata
    retained a silent fallback;
  - review fix
    `0b4c1274dc43656cb7ad502faef4f1522da35879` records XYZ as F4 with
    `single_structure_coordinates_only`, adds the exact extensionless
    basenames, requires exact fixture-family coverage, validates one ordered
    standalone marker pair, and compares the unique documented Project
    Browser export IDs with a pure-AST/no-`bpy` read of `_FORMAT_ITEMS`;
  - review-fix handoff
    `e7590817b9d405d8e98281f4f794b348d42eb986`;
  - review-fix RED: `tests.test_generated_docs_fresh` exited `1` with the
    expected XYZ, basename, marker, missing fixture-coverage and missing AST
    contract failures; the invalid-marker `--check` no-write regression
    already passed;
  - review-fix GREEN: 129 generated-doc, reader-catalog, QCSchema, XYZ/CIF/
    POSCAR/RDKit export, export-UI, MOL2/PDB readiness, documentation,
    repository and extension-validation tests passed using the existing
    shared `gemmi 0.7.5` and `rdkit 2026.03.3` path. Direct and module
    generator checks, two byte-identical generations, invalid-marker
    no-write, optional-import audit, `compileall`, UTF-8/no-BOM and line-ending
    checks, and `git diff --check` passed;
  - final independent specification and code-quality re-reviews Approved at
    exact HEAD `e7590817b9d405d8e98281f4f794b348d42eb986`
    (specification 96/96, code quality 119/119), with zero findings. Task 2
    is completed.
- `Documentation and Final Release Task 3 — Complete developer and plugin
  documentation` is awaiting independent specification and code-quality
  review;
  - implementation
    `9d277b160fcceff48a8a5faca5375739cee31cf5`;
  - RED:
    `python -m unittest
    tests.test_quantum_visualization_docs.QuantumVisualizationDocsTests.test_developer_guides_cover_reader_and_release_boundaries
    -v` exited `1` with the expected missing
    `docs/development/import-pipeline.md` failure;
  - GREEN: 220 Reader API, plugin discovery/conformance, generated
    documentation, repository and extension-validation contracts passed;
    `compileall`, direct generated-document `--check`, exact architecture path
    coverage, local-link resolution, UTF-8/no-BOM checks and
    `git diff --check` passed;
  - the four `docs/development/` guides now document the import pipeline,
    immutable source revisions, fixture provenance and the 2.3 local/remote
    Release gates; Reader API v1 RC and worker lifecycle documentation now
    point to the executable built-in, Extension and worker contribution paths;
  - runtime, manifest version, CHANGELOG and workflows are unchanged. Remote
    CI: Not Run by authorization boundary.
  - the initial code-quality review was Not Approved at handoff HEAD
    `64a3608f70f1d18e8410ff6ad33e2a53544d1c4d`: the local qualification
    command used bare Blender Python without an offline Gemmi/RDKit bootstrap,
    and the worker guide overstated host-side source/reader identity checking;
  - review fix
    `9834ca62cfbc60c6e132118750d8d5415e506f54` validates the two pinned
    wheel files through `dependency_inventory.py`, installs them only into a
    unique temporary target with `--no-index --no-deps`, probes exact versions
    through an isolated `PYTHONPATH`, and makes every documented external
    command fail closed. It never installs into Blender global site-packages;
  - the worker guide now limits host verification to request/result and
    revision UUIDs, protocol/operation/schema, safe inventory, source/document/
    artifact hashes, canonical arrays and the bundle graph. Other revision
    identity fields are explicitly owned by the fixed trusted worker and are
    not independently recomputed by the host;
  - review-fix RED round 1 failed on the missing `--no-index` contract; round 2
    failed on the missing external-command exit checks. GREEN: Task 3 focused
    verification passed 221 tests; dependency/artifact/worker verification
    passed 87 tests; the non-baseline release metadata/prerelease selection
    passed 36 tests with one skip. Seven documented CLI `--help` probes,
    generated-document `--check`, `compileall`, PowerShell syntax, links,
    UTF-8/no-BOM and `git diff --check` passed;
  - the broader 125-test dependency/release/worker invocation retained two
    baseline failures: both production-manifest byte tests expect normalized
    SHA-256 `229d1da9...`, while the unchanged manifest at baseline
    `996af1f`, handoff `64a3608` and this fix has normalized SHA-256
    `77d21636...`. The manifest and those out-of-scope tests were not changed.
  - the code-quality re-review at `ef885aa7ade25372ca3615bd651f64ee1df9ff8b`
    found one additional Important: the guide still permitted a shared
    Extension dependency path and did not remove its task-owned temp target;
  - isolation fix
    `ddcf2a178d02c71a144144943d7117a58ccd0624` removes the shared-path
    alternative. The only documented flow creates a fresh GUID child of the
    resolved OS temp parent, restores `PYTHONPATH` in `finally`, rejects a
    changed, broad, outside-parent or reparse-point cleanup target, and removes
    only that exact task root on both success and failure;
  - isolation RED failed because the old guide lacked the guarded cleanup and
    contained the shared `.local` alternative. GREEN: Task 3 focused 221 tests
    and dependency/artifact/worker 87 tests passed; seven CLI help probes,
    generated-doc `--check`, `compileall`, PowerShell syntax, links,
    UTF-8/no-BOM and `git diff --check` passed. A real offline bootstrap
    verified both pinned wheel inventories, installed and probed exact
    Gemmi/RDKit in
    `cb23-qualification-9d370f1c4ee6440a9583787664101490`, then proved that
    GUID root absent after `finally` cleanup. No global dependency path was
    changed;
  - the final specification re-review at
    `af2aacb43e748ed760911d755894b78ebcfd2aa5` found one security
    Important: an early refusal of a pre-existing GUID path left
    `$resolvedQualificationRoot` null, while the old `finally` still deleted
    any path at that name;
  - ownership fix
    `1f3aab2f6c44e4a3b8097ecc636eb59f062a28cd` starts with explicit
    `$qualificationRootOwned = $false`, creates the root itself, records and
    verifies the returned/resolved identity, parent and reparse status, and
    only then transfers cleanup ownership. `finally` cannot inspect or delete
    an unowned pre-existing root;
  - ownership RED failed on the absent flag/order contract. GREEN: Task 3
    focused 221 tests and dependency/artifact/worker 87 tests passed; seven
    CLI help probes, generated-doc `--check`, `compileall`, PowerShell syntax,
    links, UTF-8/no-BOM and `git diff --check` passed. A real Temp-only
    PowerShell simulation preserved pre-existing root
    `cb23-qualification-137e1b23ccbf46fb8a6e48f0763bf384` and its sentinel
    after early refusal, while fresh root
    `cb23-qualification-ea1562d6e6064aca8ff97f691f0986dd` was removed after
    success; the harness then safely removed its own pre-existing fixture and
    proved both paths absent. Task 3 remains open for both final reviewers.
- Next task:
  `Documentation and Final Release Task 3 — independent specification and
  code-quality review`.
- Wave 3 integration evidence:
  PR [#5](https://github.com/psiQAQ/ChemBlender_2_x/pull/5), exact HEAD
  `9124cf8660fe69ba31ec4778e7177e30821da97c`, push run `30524223438`,
  pull-request runs `30524743996` and `30524755317`, merge commit
  `7d58d1cb77037e9bc05e137e1d283f6d79c5a037`.
- Stop boundary:
  Continue through every local Wave 4 task without pausing between tasks.
  Stop only for a load-bearing implementation blocker or at the first
  operation requiring push, PR, tag, GitHub workflow dispatch, Release
  publication or other remote write without explicit authorization.
- Resume rule:
  after context compaction or a new session, read this cursor and the current
  Performance/UX plan, verify the worktree and dependency evidence live,
  and continue from the first incomplete task.
- Remote policy:
  no Wave 4 push, PR, merge, tag or Release without new explicit user
  authorization.
- Remote CI:
  `Not Run` for Wave 4.
