# ChemBlender 2.4.0 Native PDB Export

## Execution Cursor

- Goal ID: `CB240-PDB-EXPORT-T3`.
- State: `in_progress`.
- Current task: `Task 3 — Qualify model, hierarchy, loss and cancellation boundaries`.
- Selected by: `CB240-TASK3-SCOPE-DISCOVERY`.
- Baseline: `3b9846b7ec9fc6392b44d2d09918bca9a086e942`.
- Evidence:
  `docs/quantum-visualization/2.4.0/task3-candidate-intake.md`.
- Plan:
  `docs/superpowers/plans/2026-08-02-chemblender-2.4.0-pdb-export.md`.
- Existing readiness contract:
  `docs/quantum-visualization/2.3.0/specs/pdb-pqr-export-p1.md`.
- Required subgoals:
  - `deterministic-fixed-column-writer`;
  - `hierarchy-and-model-projection`;
  - `explicit-loss-preview`;
  - `atomic-cancellation-boundary`;
  - `semantic-native-reimport`;
  - `core-capability-publication`.
- Baseline verification:
  - focused PDB/docs: `67 Passed / 0 Failed`;
  - full suite with the manifest-pinned Gemmi wheel in a disposable
    `PYTHONPATH`: `2093 Passed / 26 Skipped / 0 Failed`.
- RED evidence: `python -m unittest tests.test_pdb_export_readiness
  tests.test_pdb_exporter -v` ran 13 tests; the 12 readiness tests passed and
  `tests.test_pdb_exporter` failed to import the missing `export_pdb` and
  `preview_pdb_export` functions.
- GREEN evidence: focused readiness/exporter/docs verification ran
  `42 Passed / 0 Failed`.
- Completed tasks:
  - `Task 1 — Freeze native PDB export contract`;
  - `Task 2 — Implement deterministic fixed-column PDB text`.
- Stop boundary: Complete the core native PDB exporter only. PDB UI, PQR,
  Cube, Reader API stable, dependencies, schema, version and Release work
  remain unstarted.
- Resume rule: After context compaction, read this cursor, the intake,
  readiness contract and implementation plan before editing. Continue from
  the first incomplete plan task.
- Remote policy: Push is authorized after the complete verified checkpoint;
  no PR, tag or Release is authorized.
