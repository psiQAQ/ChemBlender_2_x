# ChemBlender 2.4.0 Native PQR Export

## Execution Cursor

- Goal ID: `CB240-PQR-EXPORT-T5`.
- State: `completed`.
- Current task: `Task 6 — Exact-head remote integration gate completed`.
- Next task: `PQR Export UI`.
- Selected task: `Task 5 — Deterministic native PQR export`.
- Selected by: `CB240-TASK5-SCOPE-DISCOVERY`.
- Baseline: `c9e9e65bdbf0aa84e9e63487d60da1d5cf806362` after Task 5
  Scope Discovery and activation.
- Plan:
  `docs/superpowers/plans/2026-08-02-chemblender-2.4.0-pqr-export.md`.
- Evidence:
  `docs/quantum-visualization/2.4.0/task5-candidate-intake.md`.
- Existing readiness boundary:
  `ChemBlender/core/exporters/pdb_readiness.py::pqr_export_readiness`.

## Completed Commits

- `9254a6d569e34404f44b2f695470388aecac8da6` — deterministic native PQR
  writer, public contract, readiness identity guard and active routing.
- `12534035521c0d67ab286c9d21d2df375c9c0f9e` — printable-ASCII validation
  and direct writer-boundary identity proof.
- `f4238d68c1d9eecebeaa10fe1f881f9246568df3` — dialect, overflow,
  cancellation, loss and Semantic native re-import qualification.
- `9d9a1d832cbaa0a0e2928e39539c5b5f32b898c7` — publish PQR as
  `F5 / core / preview_confirmation` without UI activation.
- `d340aa823db46db1ba50450cc41b4ef0aac7aeef` — initial exact package
  baseline refresh, later superseded after review fixes.
- `1db60efb4f7efb19ec5c9346ba2a01dd8eedf492` — close authoritative
  snapshot, loss, hierarchy and topology qualification gaps.
- `5484a2e9319f7d3206a541833fc5dffeff4a5238` — preserve lazy snapshot
  resource ownership and cleanup.
- `6487363cd366912dca6ba0d8f4ccd60907c437f1` — preserve stable PQR
  snapshot diagnostics.
- `3a76c934a0ff132b801390662c0050dd363ee048` — stabilize rank, dtype,
  categorical and multi-issue snapshot validation.
- `e713363619faf5d9b2dccbea00c1cce9713b4969` — refresh only the final
  package/member/code artifact baselines and their exact assertions.

## RED and GREEN Evidence

- Public-contract RED: 14 tests produced one failure and one import error;
  `export_pqr` was not public and element mismatch remained `Ready`.
- Task 1–2 review RED: a DEL control label was emitted; the direct writer
  identity test failed when its trust-boundary guard was deliberately removed.
  GREEN: 20 readiness/exporter tests passed with no destination publication.
- Task 3 initial test RED: 15 tests produced two failures and one construction
  error because the tests omitted required serial-loss confirmation and used an
  invalid typed topology fixture. Correcting the tests required no production
  change; the qualified boundary suite then passed 28 tests.
- Capability RED: generated PQR export metadata was
  `F0 / none / not_available`. GREEN: the single catalog source regenerated
  `F5 / core / preview_confirmation`; PQR remained absent from Project Browser.
- Independent-review RED: both reviews were `Not Ready`, with three Important
  findings covering authoritative snapshots, loss/hierarchy semantics and
  topology association. Commits `1db60ef`, `5484a2e`, `6487363` and `3a76c934`
  closed them with targeted regressions.
- Artifact RED: the exact fresh package exceeded the old package/member/code
  baselines; the repository assertion was RED after the data update. GREEN:
  `e713363` aligned only those three final values and 20 artifact tests passed.
- Final focused GREEN: `92 Passed`, including readiness, exporter, native
  reader, generated docs and quantum-visualization docs.
- Final isolated full GREEN:
  `2156 Passed / 26 Skipped / 0 Failed` in 235.591 seconds.
- `compileall`, generated-doc freshness, optional-import audit and
  `git diff --check`: `Passed`; forbidden optional writer imports were `[]`.

## Determinism and Scientific Boundary

- Deterministic bytes:
  - with-chain output: 103 ASCII/LF bytes, two 11-field records, SHA-256
    `b3595a465830d2510ac84338a31c1844c538cda622778be0a1754c120660fb64`;
  - no-chain output: 101 ASCII/LF bytes, two 10-field records, SHA-256
    `bd3a650a0e17c6f62f3f864131a06cef5c14b4c96bda353219ef9e2b8e981603`;
  - repeated export and reversed tuple/dict container order produced identical
    bytes.
- Loss confirmation: stable sorted omissions, including model, chain segment,
  topology, cell and unsupported identity/property semantics, publish neither
  text nor destination until exact `confirm_loss=True`; atomic cancellation or
  live mutation preserves an existing destination and removes temporary files.
- Snapshot consistency: structures, hierarchies, datasets and topologies are
  enumerated once; readiness, loss preview, row construction and publication
  consume one authoritative copied snapshot, with stable family-specific rank,
  dtype and categorical diagnostics.
- Lazy ownership: preview, export, validation failure, cancellation and
  mutation restore initially unloaded `LazyNpyArray` objects to unloaded state
  while leaving caller-preloaded mappings loaded; Windows sidecar files remain
  releasable without leaked mmap/file handles.
- Hierarchy: nonzero model/segment identity requires confirmation; residue
  hetero/record-kind mismatch and conflicting native residue keys fail before
  publication. Confirmed native re-import preserves supported record, residue,
  chain, insertion-code, segment and hetero semantics.
- Topology: a bound `Structure.topology_ids` reference reports
  `topology_omitted`; missing, duplicate and cross-Structure references fail
  with `PQR topology.reference.invalid` and publish nothing.
- Semantic native re-import: native `parse_pqr()` preserved atomic numbers,
  coordinates within 0.001 angstrom, normalized hierarchy tokens, partial
  charge within 0.0001 elementary charge and radius within 0.0001 angstrom;
  UUIDs, revisions, provenance, raw lines and whitespace are intentionally not
  compared.

## Package and Blender Qualification

- Blender 5.1.2 native preflight, extension validate and build: `Passed`.
- Official ZIP: `chemblender-2.3.0.zip`, 29,969,678 bytes, SHA-256
  `c5c17470caeb85c3ce27697c66ee7ff19cb309b975b3e455129772028ad37855`.
- Exact audit: 187 unique safe members, clean CRC, no duplicates or unsafe
  paths; member-compressed 29,947,444 bytes and member-unpacked 32,034,243
  bytes.
- Unpacked sections: code 2,639,487; resources 2,506,004; wheels 26,888,752;
  other 0. Resource and wheel growth remained zero.
- Exactly two pinned wheels were present with their reviewed hashes:
  - RDKit:
    `f8bd59b24e128c9c70c975bfb1920cf610ba3096439a24ca2850eb861e767c48`;
  - Gemmi:
    `ad1f72ffa24adbfaf259e11471f6f071a668667f6ca846051f3bfea024fd337d`.
- Final artifact baselines equal the measured package/member/code bytes; every
  unexplained-growth allowance remains zero and resource, wheel and safety
  limits are unchanged. Package-CI metadata verification passed.
- Fresh isolated installed PQR smoke: module
  `bl_ext.user_default.chemblender`, native export/re-import and two
  disable/enable cycles passed with numeric exit `0`, the explicit PQR PASS
  marker and `Blender quit`.
- Repeated full lifecycle: numeric exit `0`,
  `PASS: ChemBlender extension lifecycle` and `Blender quit`. Existing Windows
  loaded-RDKit cleanup warnings did not change the functional result.

## Independent Reviews and Remote Boundary

- Final specification-compliance re-review at exact code HEAD
  `3a76c934a0ff132b801390662c0050dd363ee048`: `Ready`; no Critical,
  Important or task-related Minor findings remain.
- Final code-quality/correctness/security re-review at the same exact code
  HEAD: `Ready`; no Critical, Important or Minor findings remain.
- The following `e713363` commit changes only final artifact baselines and
  exact test assertions; its artifact tests, rebuild and package verifier all
  passed.
- Remote integration:
  - ready PR: `https://github.com/psiQAQ/ChemBlender_2_x/pull/13`;
  - exact feature head:
    `0abb4e32c6269a2a327bccfb0427c626f70084fb`;
  - `extension-package` run `30739236959`: `native-core` and `package`
    Passed with matching `headSha`;
  - `optional-qc-core` run `30739237004`: `cclib`, `iodata` and `gbasis`
    Passed with matching `headSha`;
  - ordinary merge commit:
    `54dd2364b6f935771f6d6c661452f44b7d4b558a`;
  - ancestor verification: exact feature head is an ancestor of
    `origin/main`.
- CI remediation: the first exact head failed only the artifact unpacked-size
  gate because a fresh Windows checkout expanded new LF source to CRLF. The
  final head freezes the measured `32,034,859` total and `2,640,103` code
  baselines while leaving unexplained-growth allowances and package/wheel
  limits unchanged.
- Remote CI: `Passed` for exact head
  `0abb4e32c6269a2a327bccfb0427c626f70084fb`.
- Resume rule: do not repeat native PQR core Tasks 1–6. PQR Export UI may reuse
  the completed projection, preview and writer contracts under its own plan.
- Stop boundary: native PQR core export is complete. PQR UI is a separate task;
  Cube export, Reader API v1 stable gate, model/schema/dependency/version and
  Release work remain unchanged and out of scope.
- Active/queue state: no active or queued PQR implementation remains.
