# ChemBlender 2.4.0 PQR Export UI

## Execution Cursor

- Goal ID: `CB240-PQR-EXPORT-UI-T6`.
- State: `completed`.
- Current task: `Task 6 — Exact-head remote integration gate`.
- Baseline: `54dd2364b6f935771f6d6c661452f44b7d4b558a`.
- Design:
  `docs/superpowers/specs/2026-08-02-chemblender-2.4.0-pqr-export-ui-design.md`.
- Plan:
  `docs/superpowers/plans/2026-08-02-chemblender-2.4.0-pqr-export-ui.md`.
- Core contract:
  `docs/quantum-visualization/2.4.0/pqr-export-contract.md`.
- Completed subgoals:
  - `pqr-format-choice-and-filter`;
  - `selected-pqr-entity-projection`;
  - `pqr-loss-preview-confirmation`;
  - `background-atomic-pqr-export`;
  - `project-browser-pqr-capability`;
  - `installed-pqr-export-reimport`.
- Stop boundary: PQR Export UI is locally qualified. Cube export and Reader
  API v1 stable remain unstarted.

## Completed Commits

- `9fa3af19dc887cf9996ceea8871bcb7f9a51bde6` — design the existing-UI
  integration.
- `cd9f94badef5e31fb4af5b6ba99ac0f08b5e3ed3` — activate the plan and
  persist core integration evidence.
- `8a0da1b42a2d37b2b9eb844ce880cf824573b624` — add PQR preview and
  cancellable background export through the existing operator.
- `7e0e673fe0426063c47e3b0f37050a7ff1e515a0` — publish the Project
  Browser capability and installed export/re-import smoke.
- `1682c1d51832dbdba7d2962ae4a8cc7f8dad1683` — align the exact fresh
  Windows package budgets.
- `152b84a1d19fce0206fda505fe35373ecdba05ef` — close hierarchy-label and
  fail-closed qualification findings.
- Checkpoint: `this commit`.

## RED and GREEN Evidence

- Routing RED: the active-file contract rejected the new cursor before its
  activation commit.
- UI RED: four tests failed because the format choice, filter, preview and
  background job did not support PQR.
- Capability RED: generated documents still described PQR execution as
  `core` instead of `project_browser`.
- Package RED: the fresh Windows checkout exceeded the previous exact package,
  member and code-byte baselines; no unexplained-growth allowance was raised.
- Completion RED: the single-active contract found the active cursor and the
  completed-cursor contract found no archived file before this checkpoint.
- Final focused suite: `155 Passed`.
- Final full suite: `2161 Passed / 26 Skipped / 0 Failed` in 213.615 seconds.
- Artifact contracts: `9 Passed`; generated-document freshness,
  optional-import audit, `compileall` and `git diff --check`: `Passed`.

## Product and Package Qualification

- The existing Project Browser operator exposes PQR and `*.pqr`, reuses
  `_pdb_entities()` directly, uses `preview_pqr_export()` without writing,
  and delegates the cancellable atomic job to `export_pqr()`.
- Selection excludes sibling scientific entities. Missing or partial charge,
  radius or hierarchy data fail closed; loss-bearing output requires explicit
  confirmation.
- Native re-import preserves atomic numbers, coordinates, model, chain,
  residue, alternate-location, record-kind, residue-index, atom-name, partial
  charge and radius semantics.
- Blender 5.1.2 native preflight, extension validate/build, normal
  `user_default` install and short-path isolated lifecycle: `Passed`.
- Repeated register/unregister/reload and PQR export/re-import product smoke:
  `Passed`.
- Fresh Windows checkout package:
  - package bytes: `29,969,735`;
  - member-unpacked bytes: `32,035,424`;
  - code bytes: `2,640,668`;
  - resources: `2,506,004`;
  - wheels: `26,888,752`;
  - SHA-256:
    `992217684fea523db0157b66f855298fae6cc86d178f2888711964c815965b61`.
- All artifact path, CRC, inventory, exact-wheel and zero-unexplained-growth
  checks passed.

## Independent Reviews and Remote Boundary

- Specification-compliance re-review at code head
  `152b84a1d19fce0206fda505fe35373ecdba05ef`: `Ready`; no Critical,
  Important or task-related Minor findings remain.
- Code-quality/correctness/security re-review at the same code head: `Ready`;
  no findings remain.
- Remote CI: `Not Run` for the checkpoint head. Only runs whose `headSha`
  equals the pushed exact feature head are valid.
- Remote policy: ordinary push, ready PR and ordinary merge are authorized;
  no force-push, rebase, branch deletion, tag or Release.
- Resume rule: run Task 6 only. Do not repeat core or UI implementation and do
  not begin Cube export or Reader API v1 stable.
