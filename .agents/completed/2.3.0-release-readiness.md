# ChemBlender 2.3.0 Local Release Readiness

## Result

Local final qualification: Passed on 2026-08-01.

This record qualifies the local final package only. It does not claim that the
final tag, exact-tag workflows, or public Release exist.

## Qualified Source and Metadata

- Product source commit:
  `943e8ba9dc0f2a8e803cb3449fbead3a0c5a76ad`.
- Version: `2.3.0`.
- Package: `chemblender-2.3.0.zip`.
- Checksum: `chemblender-2.3.0.sha256`.
- CI artifact name: `chemblender-2.3.0-windows-x64`.
- Normalized manifest SHA-256:
  `a48c28e6c4e56b2c859dbe7369730d1f9fbb33599e207acc2ed5f4f37473d22e`.
- The published `2.3.0-rc.1` notes remained byte-for-byte immutable after
  extraction, SHA-256
  `d19666306b6afe45c33640b8032535c713bdc6d49164f305c5d2ae5cfe4da14b`.

Subsequent readiness changes are tests and documentation only. Before final
handoff, `git diff --quiet 943e8ba9dc0f2a8e803cb3449fbead3a0c5a76ad..HEAD -- ChemBlender`
must remain successful.

## Authoritative Local Artifact

- ZIP SHA-256:
  `60585ac9fd757b53cf20947dd83c281670d3192784e23ae09537e77d9ff00ba3`.
- ZIP size: 29,956,024 bytes.
- Checksum-file SHA-256:
  `12044b5be1be7456214625372161aeb1f26a4977770bed67d9b990706b797132`.
- Checksum-file size: 88 bytes, UTF-8 without BOM.
- Inventory: 184 files, 31,975,743 unpacked bytes.
- Sections: code 2,580,988 bytes; resources 2,506,003 bytes; wheels
  26,888,752 bytes; other 0 bytes.
- Compared with the approved RC baseline, the ZIP changed by -3 bytes and the
  unpacked content by -6 bytes.
- Both the five-file `package-ci` verifier and the two-file `release-assets`
  verifier passed. Independent member-to-source comparison reported
  184 files, 0 missing, and 0 mismatched.
- Evidence retained outside the repository at `D:\cbf230-t7-943e8ba`.

Bundled dependency wheels were reused from the already-qualified local wheel
set; no package was downloaded, installed, or upgraded:

- RDKit wheel SHA-256:
  `f8bd59b24e128c9c70c975bfb1920cf610ba3096439a24ca2850eb861e767c48`.
- Gemmi wheel SHA-256:
  `ad1f72ffa24adbfaf259e11471f6f071a668667f6ca846051f3bfea024fd337d`.
- Blender bundled-Python probes passed for RDKit 2026.3.3 and Gemmi 0.7.5.

## Verification

Passed:

- Focused release/static collection: 173 tests run, 1 skipped, in
  33.205 seconds.
- Complete Blender-Python discovery: 2,056 tests run, 26 skipped, 0 failed,
  in 217.316 seconds.
- Generated-document check, `compileall`, `git diff --check`, manifest and
  release-metadata contracts.
- Blender 5.1.2 extension-native validate in 5.887 seconds and build in
  10.130 seconds.
- Artifact safe-path, CRC, dependency/license, inventory, section-size,
  unpacked-size, package-size, and exact filename checks.
- Fresh short-path Blender 5.1.2 install/register/unregister/reload lifecycle:
  `PASS: ChemBlender extension lifecycle`, exit 0, 247.120 seconds.
- Fresh default Windows `TEMP` Blender 5.1.2 lifecycle:
  `PASS: ChemBlender extension lifecycle`, exit 0, 246.536 seconds. Its 10k
  SDF measured sequence took 226.229 seconds and peaked at 126,466,639 bytes.
- Packaged legacy migration and reopen for all immutable fixtures:
  `chemblender-2.1-molecule.blend` (7.575 seconds),
  `chemblender-2.2-crystal.blend` (7.678 seconds), and
  `chemblender-2.2-edited-scaffold.blend` (7.657 seconds). Each emitted
  `PASS: packaged legacy migration and reopen` and exited 0.

The lifecycle uninstall phase logged Windows file-lock warnings for loaded
Gemmi/RDKit native modules after the functional checks. Both lifecycle runs
still emitted their explicit PASS marker and exited 0; this is retained as a
known non-functional Windows cleanup limitation.

Blender MCP live discovery was Not Run because an existing Blender 5.1 process
had no MCP listener. Direct CLI checks instead confirmed Blender 5.1.2, its
exact executable, and bundled Python 3.13 without altering the existing GUI
process.

## Remote Boundary

- Final annotated tag `v2.3.0`: Not Run in this local preparation task.
- Final exact-tag package CI: Not Run in this local preparation task.
- Final Release workflow dry-run: Not Run in this local preparation task.
- Final public Release: Not Run in this local preparation task.
- Push, PR mutation, tag creation, workflow dispatch, and Release mutation:
  none performed.

The user has explicitly authorized all remaining Git and remote operations in
this execution. No further confirmation is required for this sequence. This
execution-specific authorization does not change the repository's general
authorization policy. If any gate fails, is missing, or is ambiguous, stop
before all later steps.

1. Independent final review: Passed; record reviewed release HEAD as `H`.
2. Ordinary-push `release/2.3.0` to `origin`. Then fetch and require
   `origin/release/2.3.0 == H`.
3. Before merge, fetch and record pre-merge `origin/main` as `B`.
4. Create PR: `release/2.3.0` to `main`.
5. PR exact HEAD: all five required checks Passed:
   - `extension-package / native-core`;
   - `extension-package / package`;
   - `optional-qc-core / cclib`;
   - `optional-qc-core / iodata`;
   - `optional-qc-core / gbasis`.
6. Merge to `main` with an ordinary merge commit and record it as `M`;
   squash/rebase are forbidden.
7. Fetch and require exact merge identities:
   - `M^1 == B` (first parent equals the recorded pre-merge `origin/main`).
   - `M^2 == H` (second parent equals the reviewed release HEAD).
   - `origin/main == M`.
   - `H` is an ancestor of `M` (additional check; it does not replace either
     parent equality).
8. Main exact merge commit: all five required checks Passed.
9. Create/push annotated `v2.3.0` from the verified `origin/main` merge commit.
10. Exact-tag CI: unique successful `extension-package` run for the tag's
    exact commit.
11. Artifact verify: unexpired metadata-named five-file artifact, ZIP/checksum
    digests, inventory, licenses, size, install, and lifecycle evidence.
12. Dry-run: `extension-release.yml` with `publish=false` must pass.
13. Publish: dispatch with `publish=true`, then verify the public final Release,
    assets, body, digests, and latest status.

Only the successful final exact-tag package artifact may be published; the
Release workflow must not rebuild it.
