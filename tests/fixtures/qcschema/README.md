# QCSchema fixtures

These compact schema fixtures follow the model fields in the pinned QCElemental
source under `submodules/qcelemental`. They exercise exchange semantics and are
not scientific numerical reference calculations.
