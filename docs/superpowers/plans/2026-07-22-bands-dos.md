# Phase 2 Band Structure 与 DOS 实施计划

## Goal

完成 band/DOS/projection 的 normalized model、pymatgen-core adapter 与 Blender
Curve/linked-selection 闭环。

## Plan

1. [x] 先写 band/DOS model 的 spin、shape、energy-reference、branch、projection 与
   dangling structure tests，再实现 dataclasses 和 project validation。
2. [x] 先写 synthetic pymatgen BandStructureSymmLine/CompleteDos mapping tests，再实现
   late-import adapter、spin ordering、axis transpose 和 PDOS normalization。
3. [x] 增加可选 Vasprun reader contract，严格处理 occupation/projection availability。
4. [x] 先写 Blender Curve geometry、Fermi shift 与 selection metadata smoke，再实现最小
   electronic plot adapter。
5. [x] 更新 ADR、格式能力矩阵、路线图、依赖和 active/completed 文档。
6. [x] 运行无依赖/有依赖 CPython tests、Blender validate/build、isolated lifecycle、
   ZIP audit 和 fresh-process persistence check。
7. [x] 创建阶段提交并快进合并本地 `main`，继续 phonopy complex-mode schema。

## Verify

- band axes `(spin, kpoint, band)` 与 projection transpose 数值对照；
- DOS/PDOS axes、spin order、unit 和 volume normalization 数值对照；
- absolute energy 不变，plot shift 精确等于 Fermi energy；
- missing occupations/projections 在 report 中显式出现；
- two Python environments、Blender lifecycle、ZIP exclusions 和 `git diff --check`。
