# ChemBlender 2.5 真实教程分支：纠偏与收口任务

在现有 `PLAN_ID=2026-09-10-2.5-real-user-tutorials` 下继续执行，不另起并行大计划，不重做全部教程，不只输出新计划。总目标仍为 T00–T20/B01 的真实科学输入、用户可复做流程、GUI 证据、渲染、配对工程和中英离线交付。人工独立复做及必需阻塞项未关闭前，不得宣布整体完成。

## 审查锚点与操作权限

本次只读审查锚点为 `c1fa7584f56d6df387617406958065573e9b56c1`，分支 `feat/2.5-real-user-tutorials`。先核查本地 HEAD、工作区、当前任务/进程与此锚点差异，不强制切换或重置。如果本地已修复以下问题，用代码、测试和产物证明，不重复修复。

审查时最新候选记录为：
- Extension SHA-256：`a1e2da79253d505b60daa42aa465eb725cdd1eba00e6c81ce4082102a62d0f28`。
- prepare wheel SHA-256：`b736bc61ecdbee77f61696576c98092afc7352a9af16b4367bfd3c2e3159af3a`。
- prepare 构建源：`32d96303b2c0e16260b32b470c2d5601226d9318`。

这些是核查对象，不是无需复核的当前环境事实。保留 `docs/chemblender25-research/` 原始研究包字节不变；只修改执行副本与当前文档。保留历史输入、失败记录、旧制品、用户偏好和场景。不 push/tag/Release/PyPI，不新装或修改未经授权的依赖。最多一个 Agent-owned Blender 进程，包括后台测试；不能凭旧 PID 操作进程。正在运行的合法任务先查实际状态，不叠加新的 Blender。

## 第一阶段：先修复当前事实与验收机制

### A. 统一当前事实，但不删除历史

逐个读取当前 receipt、run manifest 和真实本地产物，不从 `status.json` 的旧汇总推断进度。修正：
- `.agents/active/2.5-real-user-tutorials.md` 顶部仍写 run-003/current phase M1，与末尾 run-009 的实操不一致。
- `examples/tutorials/2.5.0/README.md` 仍把 run-002 的工程与检查命令作为入口。
- `status.json` 的 T07 仍停在 run-004；T17/T18 仍写 not_run，尽管已有大量正向与恢复记录。
- T01/T02 的 run-003 证据必须明确标为历史候选，不能不经适用性评估就作为当前候选全通过。

保留原 `reviewed_commit` 设计锚点的含义，另明确执行源码与实际制品身份，不用最新文档提交冒充构建源码。用现有 `status.json` 汇总每案例的科学处理、真实 GUI、授权 MCP 重放、渲染、恢复、教程、审阅包、人工复做及剩余门槛；每个结论引用具体 receipt 和制品。无法找到文件或证明时保持未知/Not Run/Blocked，不凭聊天补写。

active 文件顶部只保留当前事实、当前案例/步骤、持有进程、阻塞与下一动作。将长历史迁到现有 `.planning/.../progress.md` 或相关历史文档并保留引用，不在 plan、active 中重复堆完整运行日记。计划保留里程碑、依赖和关闭标准。补上轻量一致性检查：已有被接纳的实操 receipt 的案例不能仍无解释地汇总为 not_run；机器门槛、人工门槛和分发状态不能互相代证。

### B. 修复执行版证据检查器的提前返回

`examples/tutorials/2.5.0/validate_evidence.py` 的 `_audit()` 在 `status != passed` 时立即返回，后面的制品、文件哈希、事件、科学检查和生命周期均不执行。这继承自原研究模板，不需要更改冻结研究包。

让未完成运行也检查已声明、已提交的证据完整性。未执行步骤不要求伪造对应产物；声明已经存在的文件则必须验证。分别报告证据完整性、技术执行完成、独立审阅和最终接纳，不靠先把整体状态改成 passed 才能审计。保持失败关闭原则，不降低科学或人工门槛。

至少补充以下回归：
1. blocked/running/failed 运行中的已声明文件被篡改、丢失、路径逃逸或哈希无效时，必须报告完整性失败。
2. 合法未完成运行可以完整性通过，但最终接纳仍未完成。
3. 完整性通过不能覆盖缺失 GUI/科学检查/人工审阅。
4. 历史截图不能通过改写哈希成为新制品截图。
5. 状态汇总与案例证据引用不一致时检查失败。

优先扩展现有检查器和 tests，不开发新验收平台，不维护两套状态来源。

### C. 把已批准的混合执行落实到可审计的验收规则

分支记录了用户批准原生 JPEG 与“已验证 UI 可用 MCP 重放”。不回退这一批准，也不把所有 MCP 直接算作真实 GUI。

T01 的 s01–s07 仍全部 requires_gui=true，检查器只接纳 os_gui/human_gui；目前缺少已批准复用的执行适用性表达。采用最小增量、版本化的执行补充规格，冻结旧规格与旧证据，不追改历史结果。对每个复用步骤关联：原真实 GUI 证据、当时制品身份、批准依据、当前界面/操作实现差异、当前公开 Operator 重放和科学断言。标明这是授权复用，不是本次新鼠标点击。

新面板、改变的按钮/参数/错误行为、之前没有 GUI 正向证据的步骤，仍必须当前版本真实操作和原始截图。现有人工独立复做要求不变。未经批准的豁免不能为了通过检查而自动加入。

### D. 单独处理 scientific 环境的跨环境依赖复用

`run008-scientific-route-check.json` 明确使用 `.pth` 引用已有 scientific/Standard 的 site-packages；run009 候选也声明 local dependency reuse / not portable。这些记录可以保留为本机开发环境集成证据，但不能作为独立可安装、可移交的专业后端部署资格。

原任务书禁止通过注入源码/site-packages/PYTHONPATH 补足候选环境。`python -I` 不能抵消解释器自身 site-packages 内 `.pth` 的路径扩展。逐项记录实际导入来源与已有复用方式，不把“无额外下载”写成“环境完整隔离”。

优先使用已授权、自身依赖完整的现有独立环境及固定 prepare wheel。需要额外安装、更新或环境调整但未授权时，只阻塞该专业路线的部署/用户重算门槛，列出最小授权需求；继续不依赖该路线的 Standard/Viewer 工作。不要删除既有结果，不将数值通过改成数值失败，也不要继续新增跨环境注入来关闭安装门槛。不得静默修改其他环境。

## 第二阶段：收口 P0 的技术与教学门槛

依据第一阶段的最新缺口表执行，而不是重新从 T00 开始重做每张图片。先核实 T00/T01/T02 对当前候选的适用性、真实 GUI/授权复用、可照做正文、最终工程与恢复；T02 已有 `23e46b5...` 的修复和 run-003 通过记录，不在没有新失败的情况下重复改产品。

对候选变更建立最小影响表：变更模块/科学解释/UI/公开参数、受影响案例与必须重跑项。保持历史资产原哈希，不允许自动换标。明确未受影响的部分及证据适用性后再复用；科学算法、数据格式、GUI 行为、持久化路径改变时重跑对应检查。只改教程或构图不得无理由触发所有科研数据重算；只改 prepare 的局部 reader 不自动使所有未改动的 Blender 渲染无效。原验收规则不允许复用的部分仍照实重做，不擅自放宽。

继续完成 T04/T06/T07/T17/T18 的实际未完成项。每次只推进一个案例闭环到“技术和文档具备人工复做条件”，或明确阻塞。人工等待单独排队，不阻塞无关案例，也不自动宣布整课或总计划完成。对已有本地 review ZIP 记录“本地交接通过”，不要与“最终分发未批准”混成一个不可辨认的 blocked。

T07 当前已做到 run-009 的 Cube GUI→Viewer 重放、切片/剖面/色标、差分、渲染和若干冷重开/重建。补缺口，不重新按旧 status 的初始转换阶段执行。T17 的 13 格式正向导出和 T18 的 Save As/Relink 等已存在记录，应评估当前候选适用性，而不是再次声称尚未开始。

## 第三阶段：把正文整理成用户教程，而不是运行日志

每个案例的当前正文只保留一套明确版本和流程。将旧候选说明、失败调试、重复冷启动 PID、长哈希审计列表移到案例验证附录，保持原记录和链接。尤其处理 `first-aspirin.md`、`ethanol-conformers.md` 的旧候选，以及 `aspirin-trajectory.md` 顶部 run-009 与后部旧候选步骤并列的问题；中英同步。

正文必须包含可获得的输入、前提、准确按钮/参数、每步可观察结果、逐步图片、最终成图、配对工程/本地审阅包入口、恢复和科学边界。内部 `.blend-analysis` 路径只能出现在开发验证附录；本地交接包应有包内相对路径和入口，不能要求读者拥有作者缓存。未发布时不虚构互联网下载地址。

精细图像仍属于任务范围，但先定义可判定质量条件：标签和单位可读、主体没有意外裁切、颜色/相位正确、没有重复几何或影响解释的严重遮挡。自定义节点由 MCP 搭建的，就明确补手动步骤或提供含节点的合法工程，不写成已手工验证。区分显示插值/细分和更密科学网格，Rebuild 后自定义外观丢失应有可照做恢复步骤。不要在没有具体缺陷的情况下反复增加样本或调整灯光。

针对本阶段实际变化执行相关测试与真实离线页面检查，验证图片、锚点、输入/工程链接或本地包入口，而非只增加截图计数。完成一个案例后同步状态、目录与对应证据。

## 后续与阶段结束输出

保持 P0→P1→P2 主次；P0 的技术/文档门槛具备人工复做条件或阻塞明确后，推进 T03/T05/T08–T15，再到 T16/T19/T20/B01。P1/P2 仍属于总目标，不因整改而删除。高级案例保留“已有合法 CBQ 的 Viewer 路线”和“明确后端条件的重算路线”。QCSchema 交换不代替计算，provider unavailable 不代替真实取数，Fermi 许可未闭合不分发输入。

每个逻辑阶段给出：已修正内容、实际测试命令及结果、当前制品适用性、按案例/轨道汇总的状态、明确的未解决项和下一动作。最终同时满足科学、用户操作、图文教程、工程恢复、依赖/许可、独立人工复做等相应门槛才能关闭总目标。仓库外发布仍需单独授权。
